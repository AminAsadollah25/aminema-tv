# Aminema 0.18.4 — Live TV Health Tabs

## تغییرات

- تب اصلی **کانال‌های فعال** فقط کانال‌هایی را نشان می‌دهد که از صفحهٔ عادی
  Provider، پاسخ قابل پخش گرفته‌اند.
- تب **همه کانال‌ها** برای دیدن و عیب‌یابی همهٔ کانال‌های پیکربندی‌شده باقی می‌ماند؛
  حتی اگر فعلاً غیرفعال یا هنوز بررسی‌نشده باشند.
- هنگام ورود به پخش زنده، بررسی کانال‌ها در پس‌زمینه شروع می‌شود و شمارندهٔ پیشرفت
  بدون بستن صفحه نمایش داده می‌شود.
- هر کانال در هر نوبت فقط یک تلاش دارد؛ حلقهٔ تلاش مجدد یا کلیک تکراری اضافه نمی‌شود.
- تکراری‌های بین Providerها یکی می‌شوند. منبع فعال برنده است؛ در وضعیت برابر
  اولویت با ParsiFlix، سپس ParsaTV و بعد BabakTV است.
- وضعیت و زمان آخرین بررسی محلی ذخیره می‌شود و پس از ۳۰ دقیقه دوباره قابل بررسی است.
- هیچ URL مدیا، توکن، DRM، Cookie، Login یا WebStorage در Health Check استخراج یا
  ذخیره نمی‌شود؛ پخش عادی همچنان از BrowserActivity و صفحهٔ خود Provider استفاده می‌کند.

## سازگاری

- تغییرات قبلی ورود، کوکی، ادامهٔ تماشا و مسیرهای پخش حفظ شده‌اند.
- همهٔ Providerهای Live TV موجود در `services.json` در بررسی و حذف تکراری شرکت می‌کنند؛
  بنابراین کانال‌های BabakTV هم از تب «همه» ناپدید نمی‌شوند.

versionCode: 46
