# Aminema 0.12.0

## پخش زنده، این بار واقعاً یک‌کلیکی

دکمه واسط نسخه قبل از هدر حذف شد و یک ردیف بومی و سینمایی **پخش زنده**
پایین Home اضافه شد. این ردیف ۲۰ شبکه را با لوگوی واقعی، نشان LIVE و
انیمیشن فوکوس مناسب کنترل تلویزیون نمایش می‌دهد.

انتخاب هر کارت مستقیماً همان شبکه را باز می‌کند و ویدئو بدون رفتن به صفحه
فهرست سایت یا نیاز به کلیک دوم، تمام صفحه تلویزیون را می‌گیرد. پخش،
ورود و نشست کاربر همچنان کاملاً در اختیار صفحه عادی خود سرویس است.

## کنترل و موس

- پیمایش افقی ۲۰ شبکه با D-pad و اسکرول خودکار ردیف
- بازکردن با OK کنترل یا کلیک موس
- Back مستقیم به Home و همان کارت فوکوس‌شده
- کنترل‌های استاندارد HTML5 برای موس همچنان در دسترس‌اند

## معماری قابل‌گسترش

مدل `liveTv.channels` به `services.json` اضافه شد. نام، مسیر صفحه و لوگوی
شبکه‌ها در JSON قرار دارند؛ برای افزودن شبکه یا منبع آینده نیازی به تغییر
طراحی Home نیست. مسیرها با حساب لاگین‌شده واقعی کشف و آزمایش شدند و هیچ
لینک مدیا، توکن، DRM یا اطلاعات احراز هویت خوانده یا ذخیره نمی‌شود.

کد نسخه `25`، شناسه برنامه و امضا بدون تغییر.

---
versionCode: 25
