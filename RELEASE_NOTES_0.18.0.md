# Aminema 0.18.0 — Live TV Playback Polish

## خلاصه

در این نسخه بخش پخش زندهٔ Aminema با تمرکز روی تجربهٔ واقعی Android TV تکمیل
شد: ورود سریع‌تر به پلیر، پخش خودکار، تمام‌صفحه‌شدن بدون کلیک اضافه و fallback
صادقانه برای کانال‌هایی که سرور یا پلیر متفاوت دارند.

## تغییرات اصلی

- اضافه‌شدن مقصد مستقل **پخش زنده** برای کانال‌های ParsaTV.
- دسته‌بندی فارسی، سراسری و صداوسیما، ورزشی و سایر شبکه‌ها.
- استفاده از Embed رسمی خود ParsaTV برای حذف بار اضافی صفحهٔ دایرکتوری.
- پخش خودکار از همان کلیک روی کانال.
- پشتیبانی از پلیرهای HTML5، JWPlayer و iframeهای هم‌دامنه.
- تمام‌صفحه‌شدن خودکار پلیر بدون نیاز به کلیک دستی روی دکمهٔ Fullscreen.
- تشخیص پخش واقعی قبل از مخفی‌کردن صفحهٔ اتصال؛ قاب خالی یا iframe صرفاً
  به‌عنوان پخش موفق پذیرفته نمی‌شود.
- شناسایی فقط کنترل‌های واقعی Server/Source/Mirror/Backup برای fallback.
- حذف استفادهٔ اشتباه از JW7/HLS/HTML5 به‌عنوان سرور جایگزین کانال.
- فیلتر تبلیغات MGID فقط در محدودهٔ ParsaTV.
- حفظ کوکی‌ها، Login، Local Storage، Library و Sessionهای قبلی.

## QA

- `testDebugUnitTest` موفق.
- `lintDebug` موفق.
- `assembleDebug` موفق.
- تست نصب با `adb install -r` بدون پاک‌سازی داده.
- تست موس و D-pad/OK روی امولاتور Android TV.
- تست واقعی Pars TV، Iran National Revolution TV، Iran International TV و
  DEJ TV با پخش تمام‌صفحه.
- APK در مسیر انتشار با نسخه `0.18.0` و `versionCode: 42` ساخته شده است.

## مرزهای امنیتی

Aminema فقط صفحات عادی سایت را در WebView نمایش می‌دهد. این نسخه هیچ Media URL،
Token یا مقدار DRM را استخراج، ذخیره، لاگ یا بازتوزیع نمی‌کند و احراز هویت را
دور نمی‌زند.

versionCode: 42
