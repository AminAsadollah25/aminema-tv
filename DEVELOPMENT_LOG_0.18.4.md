# Development Log — Aminema 0.18.4

## هدف

نمای اصلی Live TV باید به‌جای فهرست خام، کانال‌هایی را جلو بیاورد که واقعاً
قابل پخش‌اند؛ اما فهرست کامل برای عیب‌یابی و مشاهدهٔ کانال‌های موقتاً غیرفعال
همچنان در دسترس بماند.

## پیاده‌سازی

- `LiveChannelHealthProbe` یک WebView جدا و متصل به Activity دارد و فقط وضعیت
  پخش HTML5/JWPlayer را بررسی می‌کند: `readyState`، حالت paused، زمان پخش و
  فریم رمزگشایی‌شده. URL مدیا و توکن‌ها خوانده یا Log نمی‌شوند.
- هر کانال در هر Refresh یک بار بررسی می‌شود؛ Timeout همان تلاش به وضعیت
  `INACTIVE` تبدیل می‌شود و Retry Loop وجود ندارد.
- `LiveChannelHealthRepository` فقط `status` و `checkedAt` را در Preferences
  ذخیره می‌کند و TTL وضعیت ۳۰ دقیقه است.
- `LiveChannelCatalog` نام‌ها را نرمال می‌کند و تکراری‌ها را یکی می‌کند. منبع
  فعال بر منبع نامشخص/غیرفعال اولویت دارد؛ در تساوی اولویت Provider ثابت است.
- `LiveTvActivity` تب فعال را به‌عنوان تب اصلی نمایش می‌دهد، تب همه را برای ممیزی
  نگه می‌دارد، و بررسی دستی/پیشرفت پس‌زمینه را اضافه می‌کند.

## محدودیت عمدی

بررسی کامل ۲۸۷ رکورد خام فعلی ممکن است بسته به زمان پاسخ Provider طول بکشد؛ این
فرایند عمداً متوالی است تا روی Android Box فشار هم‌زمانی و درخواست مجدد ایجاد
نکند. صفحهٔ Live TV در تمام این مدت قابل استفاده است.

## حفظ وضعیت

هیچ uninstall، clear-data، حذف Cookie یا Logout در توسعه و تست این نسخه انجام
نمی‌شود.

versionCode: 46
