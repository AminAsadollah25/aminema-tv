# Aminema 0.9.2

## اصلاح — پخش مستقیم از Continue Watching

آیتم‌هایی که از **Sync حساب** آمده‌اند آدرس صفحه پخش ذخیره‌شده ندارند، فقط
صفحه محتوا را دارند. برای همین کلیک روی آن‌ها فقط صفحه جزئیات را باز می‌کرد و
پخش مستقیم اتفاق نمی‌افتاد.

حالا برای همین آیتم‌ها (آن‌هایی که `playbackUrl` ندارند) همان مسیر پخش
مستقیم 0.8.2 فعال می‌شود: بهترین گزینه پخش آنلاین خود سایت انتخاب و باز
می‌شود — دوبله در اولویت، کیفیت ۱۰۸۰ ← ۷۲۰ ← ۴۸۰، و ۲۱۶۰ هرگز.

آیتم‌هایی که خودت این‌جا پخش کرده‌ای بدون تغییرند، چون از قبل آدرس صفحه پخش
را ذخیره دارند و مستقیم به همان‌جا می‌روند.

همچنین کلیک روی دکمه «ادامه تماشا»ی سایت دیگر بعد از پخش مستقیم انجام نمی‌شود،
تا صفحه دوبار جابه‌جا نشود.

## تصحیح یک برداشت اشتباه در گزارش قبلی

در گزارش 0.8.2 نوشته بودم بیشتر فیلم‌های «تازه‌های خارجی» نسخه پخش آنلاین
ندارند. **این اشتباه بود.** بازرسی دوباره نشان داد هر ۸ فیلم فهرست
`new-films` — از جمله Colony که مثال زده بودم — گزینه پخش آنلاین دارند.

علت اشتباه: آن بازرسی دنبال لینک `<a href="/stream/…">` می‌گشت، در حالی که
دکمه‌های پخش `div` با `onclick` هستند و لینک نیستند.

کد نسخه `20`، شناسه و امضا بدون تغییر.
