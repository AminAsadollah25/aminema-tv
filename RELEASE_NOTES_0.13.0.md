# Aminema 0.13.0 — Keyboard Edition

## Search Deck جدید

- کیبورد جستجوی خود Aminema به یک پنل سینمایی یکپارچه تبدیل شد؛ عبارت،
  زبان و تعداد حروف داخل همان Deck دیده می‌شوند.
- چیدمان فارسی و انگلیسی اکنون QWERTY واقعی و پلکانی است.
- حروف پرکاربرد فارسی مثل **ژ** و **آ** بدون Shift یا Long press در دسترس‌اند.
- کلیدها اندازه ثابت، Hover موس و Focus واضح ریموت دارند.
- دکمه قرمز جستجو تا قبل از واردکردن دو حرف غیرفعال می‌ماند.
- بعد از شروع جستجو، کیبورد به یک نوار فشرده تبدیل می‌شود تا فضای بیشتری
  برای پوسترها و نتایج ایرانی و خارجی آزاد شود.

## Input Deck جدید برای ورود به سایت‌ها

- ظاهر کیبورد ورود کاملاً بازطراحی شد: نشان واضح **نام کاربری**،
  **رمز عبور** یا **جستجو**، Preview ثابت و دکمه مستقل **بستن**.
- مسیر ورود ساده‌تر است:
  `نام کاربری → بعدی → رمز عبور → ورود`
- کنار Username راهنمای **بعدی: رمز** دیده می‌شود.
- Password دارای نمایش/پنهان‌کردن واضح است و متن طولانی دیگر ابعاد Header
  را به‌هم نمی‌ریزد.
- QWERTY فارسی و انگلیسی، Caps، Space، Backspace و کلیدهای ایمیل برای موس
  USB و کنترل DPAD آماده‌اند.

## رفع بنیادی باگ Focus و Password

- هر فیلد سایت و هر بار بازشدن کیبورد اکنون شناسه مستقل دارد.
- رویداد قدیمی یا Focus ناخواسته دیگر نمی‌تواند تایپ Password را به Username
  برگرداند.
- تغییر Caps، زبان یا Show/Hide ردیف‌های کیبورد را حذف و بازسازی نمی‌کند.
- Next حتی وقتی سایت فرم Login را هنگام تایپ بازسازی کند، Password را پیدا
  و قفل می‌کند.
- Back فقط کیبورد را می‌بندد و کاربر دیگر داخل صفحه Login گیر نمی‌کند.
- کیبورد سیستمی Android هنگام استفاده از Input Deck باز نمی‌شود.

## تست و حریم خصوصی

- Search Deck و Input Deck روی Android TV 1920×1080 با موس و DPAD بررسی شدند.
- زنجیره Username، Next، Password، Caps، فارسی، Show، Submit و Back روی یک
  فرم محلی داخل WebView تست شد.
- برای تست، حساب‌های واقعی کاربر Logout نشدند و هیچ رمز، Cookie یا Token
  خوانده یا ذخیره نشد.
- Build و Lint نهایی بدون خطا انجام شدند.

---
versionCode: 27
