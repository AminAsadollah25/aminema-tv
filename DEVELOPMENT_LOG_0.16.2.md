# گزارش توسعه Aminema 0.16.2

## مسئله واقعی

Hero منتخب گاهی یک بنر 16:9 را داخل قاب پوستر عمودی می‌کشید. بعضی سریال‌های
ایرانی و خارجی نیز با وجود داشتن خلاصه در یکی از ردیف‌های Provider، به‌علت
ادغام `first-wins` بدون خلاصه نمایش داده می‌شدند. برای اطلاعات قدیمی‌تر هم
Provider همیشه سال، عوامل و Synopsis کامل نداشت. صفحه جستجو نیز Back
قابل‌دیدن و مقصد مطمئن در حالت Task restore نداشت.

## تصمیم محصول و UX

- در Hero، پوستر واقعی foreground است و بنر واقعی فقط ambient backdrop.
- صفحه فارسی از نظر هندسه و Navigation کاملاً RTL است؛ اکشن اصلی سمت راست و
  «بعدی» سمت چپ قرار می‌گیرد.
- منبع اطلاعات اول Provider است؛ منبع عمومی فقط فیلد خالی را تکمیل می‌کند.
- نتیجه‌ای که هویت، سال یا نوع آن قابل‌تطبیق نیست رد می‌شود؛ نبود داده بهتر
  از داده ساختگی است.
- Enrichment عمومی فقط در Spotlight انجام می‌شود تا Home سبک بماند.

## تغییرات فنی

- `CinematicHero.kt`: پوستر عمودی واقعی در foreground، بنر عریض در background،
  RTL صریح، اندازه‌های محدود Coil و Compose فقط برای اسلاید فعال.
- `HomeScreen.kt`: اتصال Featured به پوستر Catalog با URL canonical، ادغام
  variantهای تکراری و تکمیل Continue/Spotlight از Catalog cache.
- `CatalogBackgroundSync.kt`: Hydrate کردن Featured پارسی‌فلیکس و FilmRooz با
  پوستر واقعی، خلاصه و Metadata صفحه Detail؛ بنر در `backdropUrl` حفظ می‌شود.
- `TitleMetadata`: فیلدهای `imdbId` و `externalLookupAt`، Merge غیرمخرب و
  معیار `isDecisionComplete()`.
- `CatalogRepository`: ذخیره Metadata به‌صورت Merge و نرمال‌سازی IMDb ID.
- `SpotlightMetadataLoader`: Retry برای SPA، selectorهای قوی‌تر Synopsis،
  فیلتر متن منو/دانلود/لاگین و استخراج IMDb ID فقط از صفحه عادی Provider.
- `PublicTitleMetadataEnricher.kt`: fallback تازه Wikipedia/Wikidata با
  Persian-first، تطبیق عنوان+سال+نوع، QID/IMDb exact match، نام فارسی عوامل،
  Cache منفی ۳۰روزه و عدم ارسال اطلاعات نشست.
- `SpotlightActivity.kt`: Provider-first سپس public fill-only و Cache نتیجه.
- `SearchActivity.kt`: Back pill قابل Focus/Click و Home fallback مشترک.
- تست تازه `TitleMetadataMergeTest` برای جلوگیری از پاک‌شدن Metadata غنی.

## باگ ریشه‌ای کشف‌شده

یک عنوان ممکن بود هم در `series` و هم در `popular` باشد. نسخه قبلی با
`distinctBy` اولین رکورد را نگه می‌داشت؛ اگر آن رکورد خلاصه نداشت، خلاصه کامل
رکورد بعدی حذف می‌شد. اکنون همه variantهای URL canonical با انتخاب طولانی‌ترین
خلاصه و کامل‌ترین فیلدها Merge می‌شوند.

## مرز امنیتی و داده

هیچ لینک مدیا، Cookie، Token، Password یا DRM خوانده، ذخیره یا برای منابع
عمومی ارسال نمی‌شود. IMDb Scrape نمی‌شود؛ فقط شناسه عمومی IMDb که خود Provider
نمایش داده باشد برای تطبیق Wikidata استفاده می‌شود. Provider همیشه بر fallback
عمومی اولویت دارد.

## محدودیت صادقانه

اگر Provider عوامل را ندهد و Wikipedia/Wikidata نیز تطبیق قابل‌اعتماد نداشته
باشند، Aminema آن فیلد را خالی می‌گذارد. نمونه واقعی «کوری» خلاصه کامل دارد،
اما عوامل آن بدون منبع معتبر ساخته نشده‌اند.

## QA واقعی

- Hero منتخب `Tuner`: پوستر واقعی عمودی، بنر پس‌زمینه و خلاصه فارسی.
- `House of the Dragon`: خلاصه Provider، سال/ژانر و کارگردان/بازیگران فارسی
  تکمیل‌شده با Wikidata و IMDb ID دقیق.
- سریال ایرانی `کوری`: پوستر، backdrop، سال، ژانر و خلاصه فارسی.
- `The Hawk`: رفع حذف خلاصه ناشی از duplicate variant.
- Continue عنوان `Michael`: تکمیل خلاصه و artwork از Catalog cache.
- نصب درجا نشست‌ها را حفظ کرد و FATAL EXCEPTION صفر بود.

## قدم بعدی پیشنهادی

`0.16.3 — Episode Navigator`: نمایش فصل/قسمت و سه مسیر روشن «ادامه قسمت
بعد»، «آخرین قسمت منتشرشده» و «انتخاب قسمت»، با Evidence واقعی هر Provider.

## تکمیل نهایی Spotlight و Hero — ۱ اوت ۲۰۲۶

- Hero فقط سال را نشان می‌دهد؛ عبارت‌های «شمسی/میلادی» حذف شدند. سال ایرانی
  با رقم فارسی و سال خارجی با رقم لاتین نمایش داده می‌شود.
- چیپ «محصول کشور» از Hero حذف شد تا تصمیم سریع و خلوت بماند؛ کشور و زبان
  همچنان در صفحه کامل Spotlight دیده می‌شوند.
- فلش‌های Hero از روی متن و Synopsis برداشته و به یک کنترل جمع‌وجور پایینِ
  چپ منتقل شدند؛ دیگر هیچ متن یا دکمه‌ای را نمی‌پوشانند.
- Spotlight اکنون Synopsis را تا چهار خط، کارگردان تا سه نفر، بازیگران تا
  هشت نفر، کشور، زبان، مدت، دوبله/زیرنویس و ژانرها را به شکل فشرده نشان می‌دهد.
- Rating خارجی به نشان کیفیت IMDb تبدیل شد: زیر ۵ زرشکی/ضعیف، ۵ تا ۷
  کهربایی/متوسط، ۷ تا ۹ سبز/خوب و ۹ به بالا طلایی/عالی. ارقام فارسی و عربی
  نیز پیش از ارزیابی به لاتین تبدیل می‌شوند.
- سال تکراری انتهای عنوان خارجی حذف می‌شود؛ سال فقط یک‌بار در Badge مستقل
  دیده می‌شود.

### تکمیل واقعی عوامل ایرانی

`SheydaMetadataLoader.kt` به‌عنوان fallback رسمی و عمومی اضافه شد. چون HTML
اولیه شیدا فقط پوسته SPA است، یک WebView نامرئی سبک دقیقاً یک جستجوی عمومی
انجام می‌دهد، فقط تطبیق دقیق عنوان + نوع فیلم/سریال را می‌پذیرد و سپس از UI
عادی صفحه عنوان این فیلدها را می‌خواند: خلاصه، سال شمسی، ژانر، تصویر افقی،
کارگردان و بازیگران. هیچ ورود، Player، Media URL یا اطلاعات نشست استفاده
نمی‌شود.

پذیرش واقعی با «بامداد خمار» موفق بود:

- سال `۱۴۰۳`
- کشور ایران و زبان فارسی
- کارگردان: نرگس آبیار
- بازیگران: علی مصفا، رضا کیانیان، لاله اسکندری، گلاره عباسی
- Synopsis، ژانرهای فارسی و Backdrop افقی رسمی

نسخه lookup cache از ۴ به ۵ افزایش یافت تا رکوردهای ناقص قبلی یک‌بار با این
منبع تازه دوباره تکمیل شوند. نبود تطبیق دقیق همچنان به معنی فیلد خالی است، نه
حدس.
