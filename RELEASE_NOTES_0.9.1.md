# Aminema 0.9.1

دو اصلاح روی رفتارهای نسخه‌های قبل.

## اصلاح ۱ — پخش مستقیم فیلم ایرانی

نسخه 0.8.2 روی صفحه فیلم ایرانی دکمه «پخش» را می‌زد، ولی آن صفحه یک بخش
**پخش زنده شبکه‌های تلویزیونی** هم دارد و گاهی آن اجرا می‌شد.

- الگوهای کلیک محدود شد به «ادامه تماشا» و «تماشا»
- فهرست استثنا اضافه شد: `زنده`، `شبکه`، `تلویزیون`، `رادیو`، `live` —
  دکمه‌ای که این کلمات را داشته باشد هرگز زده نمی‌شود

همراهش یک اشکال جدی‌تر هم پیدا و رفع شد: تنظیم `directPlay` بعد از اولین
نصب در فایل کاربر ذخیره می‌ماند و نسخه جدیدِ همان تنظیم به دستگاه نمی‌رسید.
یعنی این اصلاح هیچ‌وقت روی تلویزیون فعال نمی‌شد. حالا نسخه همراه برنامه
همیشه جایگزین می‌شود (دقیقاً مثل `resumeStrategy`).

## اصلاح ۲ — دکمه «ادامه تماشا»ی سلام Home

قبلاً مستقیم می‌رفت داخل آخرین فیلم. حالا فقط صفحه را تا ردیف
**Continue Watching** اسکرول می‌کند و انتخاب با خودتان است.

## بروزرسانی

کد نسخه `19`، شناسه و امضا بدون تغییر.
