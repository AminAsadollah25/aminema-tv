# Aminema 0.10.0

## بروزرسانی خودکار داخل برنامه

از این نسخه به بعد، دیگر لازم نیست فایل را از گوگل‌درایو دانلود و دستی نصب
کنید. برنامه در Cold Start خودش نسخه جدید را روی گیت‌هاب چک می‌کند.

- اگر نسخه جدیدتری منتشر شده باشد، یک نوار کوچک بالای Home ظاهر می‌شود:
  «نسخه X.Y.Z موجود است» با دو دکمه **بروزرسانی** و **بعداً**
- «بروزرسانی» را بزنید: دانلود با نوار پیشرفت انجام می‌شود، فایل با
  **sha256** منتشرشده مقایسه می‌شود، و بعد صفحه نصب استاندارد خود اندروید
  باز می‌شود
- «بعداً» را بزنید: همان نسخه دیگر در این چرخه نشان داده نمی‌شود؛ نسخه بعدی
  که منتشر شود دوباره اطلاع می‌دهد
- در Settings ➜ About هم دکمه **بررسی بروزرسانی** برای چک دستی اضافه شد

## چیزی که عمداً خودکار نیست

نصب هرگز کاملاً بی‌صدا نمی‌شود — اندروید همیشه یک تأیید نهایی از شما
می‌خواهد، مگر برنامه در سطح سیستم باشد که Aminema عمداً نیست. این یعنی یک
Build خراب هرگز نمی‌تواند خودش را بدون اطلاع شما نصب کند.

اولین بار که «بروزرسانی» را بزنید، اندروید از شما اجازه «نصب از منابع
ناشناس» را برای Aminema می‌خواهد — این فقط یک‌بار لازم است.

## منبع بروزرسانی

آدرس ثابت است و به مخزن گیت‌هاب پروژه اشاره دارد؛ فایل جداگانه‌ای برای
مانیفست بروزرسانی لازم نیست چون خود API انتشارهای گیت‌هاب همه‌چیز (نسخه،
لینک APK، توضیحات) را می‌دهد.

## توجه درباره حجم APK

این نسخه هنوز با همان Build دیباگ فعلی (بدون فشرده‌سازی R8) منتشر شده تا
شناسه بسته (`com.amin.tvos.debug`) و امضا با نصب فعلی روی تلویزیون یکسان
بماند و مستقیم Update شود. فشرده‌سازی با R8 — که حجم را به‌طور محسوس کم
می‌کند — به یک نسخه جدا و با تست دقیق‌تر موکول شد، چون تغییری در سطح کل
Build است و نمی‌شود فقط با امولاتور از سلامت کامل آن مطمئن شد.

کد نسخه `22`، شناسه و امضا بدون تغییر.

---
versionCode: 22
