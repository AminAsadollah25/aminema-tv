# Aminema 0.9.3

تکمیل پخش مستقیم فیلم برای دو ردیف باقی‌مانده: **اخیراً بازشده** و
**علاقه‌مندی‌ها**.

## تغییر

این دو ردیف قدیمی‌تر از فیلد نوع‌محتوای کاتالوگ هستند و نمی‌دانستند آیتم
ذخیره‌شده فیلم است یا سریال. حالا نوع محتوا مستقیم از روی شکل خود آدرس صفحه
تشخیص داده می‌شود (`/medias/movies/` یا `/medias/series/` برای فیلم ایرانی،
`/post/film/` یا `/post/series/` برای فیلم خارجی) — همان الگویی که Adapterها
از قبل برایش تنظیم شده‌اند.

نتیجه: کلیک روی یک **فیلم** در Recently Opened یا Favorites حالا مستقیم به
صفحه پخش سایت می‌رود؛ کلیک روی یک **سریال** طبق طراحی هنوز روی صفحه جزئیات
می‌ماند تا انتخاب خودکار قسمت در نسخه بعدی بیاید.

## تست

روی امولاتور با هر چهار مسیر تأیید شد:

- کارت فیلم در «تازه‌های خارجی» ← پخش مستقیم
- Continue Watching (هم پخش محلی، هم Sync حساب) ← پخش مستقیم
- **Recently Opened** — فیلم (Colony) ← پخش مستقیم؛ سریال (Gone) ← صفحه جزئیات
- **Favorites** — فیلم (Colony) ← پخش مستقیم

بدون Crash در Logcat.

کد نسخه `21`، شناسه و امضا بدون تغییر.
