# Aminema 0.7.4

نسخه 0.7.4 یک Maintenance Update کوچک روی نسخه نصب‌شده 0.7.3 است و فقط
اینتروی برند را اضافه می‌کند. هیچ تغییری در Login، Cookies، سرویس‌ها،
Continue Watching، Favorites، کیبورد یا مرورگر انجام نشده است.

## تغییرات

- افزودن **اینتروی Aminema** با ویدئوی نهایی CapCut، فقط در Cold Start
- ویدئو به‌صورت Local داخل APK قرار دارد (`res/raw/aminema_intro.mp4`) و هیچ
  اتصال شبکه‌ای برای اینترو انجام نمی‌شود
- ردکردن فوری با `OK`، `Enter`، `DPAD_CENTER`، `Back` یا کلیک موس
- عدم پخش دوباره هنگام برگشت از مرورگر، Sync یا Settings
- بدون Loop؛ پس از پایان ویدئو Home باز می‌شود
- Home هم‌زمان با اینترو ساخته می‌شود و بلافاصله پس از پایان یا Skip آماده است
- خطای Decoder، فایل خراب یا دستگاه کند باعث توقف برنامه نمی‌شود؛ در خطا Home
  فوراً باز می‌شود
- صدا از تنظیم Media دستگاه پیروی می‌کند
- دو گزینه جدید در Settings ➜ **Startup Intro**:
  - `Play intro` برای روشن/خاموش‌کردن اینترو
  - `Mute intro` برای پخش بی‌صدا

## بروزرسانی و حفظ اطلاعات

- شناسه برنامه بدون تغییر: `com.amin.tvos.debug`
- کد نسخه جدید: `14`
- گواهی امضای APK با نسخه 0.7.3 یکسان است
  (`SHA-256: ba6ac8c4…38ba13d3`)، بنابراین روی برنامه نصب‌شده Update می‌شود.
- کوکی‌ها، ورودها، تنظیمات، Continue Watching، Favorites و History پاک
  نمی‌شوند.

برای بروزرسانی، APK جدید را روی همان برنامه نصب‌شده اجرا کنید و گزینه
`Update` را بزنید. نسخه قبلی را Uninstall نکنید.

## حجم

ویدئوی اینترو حدود 10.6 مگابایت است و حجم APK دیباگ از حدود 64 مگابایت به
حدود 75 مگابایت افزایش پیدا می‌کند.
