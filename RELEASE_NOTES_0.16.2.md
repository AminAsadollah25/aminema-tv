# Aminema 0.16.2 — Cinematic Hero, Complete Metadata & Search Polish

این نسخه Hero و صفحه جزئیات را از یک پوسته زیبا به یک تجربه واقعاً
اطلاعات‌محور و سینمایی تبدیل می‌کند و مسیر برگشت مطمئن جستجو را نیز اضافه
می‌کند.

## Hero با پوستر واقعی

- عنوان‌های منتخب دیگر بنر عریض را به‌جای پوستر نشان نمی‌دهند؛ تصویر عمودی
  واقعی در سمت راست دیده می‌شود و بنر عریض فقط پس‌زمینه سینمایی است.
- ساختار کاملاً RTL است: اطلاعات راست‌چین، اکشن اصلی سمت راست و «بعدی» سمت
  چپ با فلش درست.
- تصویر پس‌زمینه، پوستر، عنوان، چیپ‌ها و خلاصه داخل Safe Area تلویزیون باقی
  می‌مانند و حرکت‌ها کوتاه و آرام‌اند.
- رکوردهای تکراری یک عنوان در ردیف‌های تازه‌ها/سریال‌ها/برگزیده‌ها اکنون
  ادغام می‌شوند؛ خلاصه یا عوامل کامل دیگر با یک رکورد ناقص پاک نمی‌شوند.

## اطلاعات کامل‌تر فیلم و سریال

- خلاصه سریال‌های ایرانی و خارجی از صفحه عادی و لاگین‌شده همان سرویس با
  Retry مناسب برای صفحه‌های SPA دریافت می‌شود.
- منبع اصلی همیشه خود سرویس است. اگر خلاصه، سال، کارگردان یا بازیگران ناقص
  باشند، Aminema فقط فیلدهای خالی را از Wikipedia/Wikidata عمومی تکمیل
  می‌کند و داده Provider را بازنویسی نمی‌کند.
- در صورت وجود IMDb ID روی صفحه سرویس، همان شناسه برای تطبیق دقیق Wikidata
  استفاده می‌شود؛ خود IMDb Scrape نمی‌شود.
- نام کارگردان و بازیگران تا جای ممکن فارسی است و نبود منبع قابل‌اعتماد با
  حدس یا متن ساختگی پر نمی‌شود.
- نتیجه‌های ناقص قبلی با Metadata تازه ادغام می‌شوند و Cache منفی ۳۰روزه از
  درخواست‌های تکراری برای عنوان‌های بدون نتیجه جلوگیری می‌کند.

## بازگشت مطمئن از جستجو

- یک دکمه واضح «بازگشت» بالای صفحه جستجو اضافه شد که با ریموت و موس کار می‌کند.
- Back ریموت و دکمه روی صفحه از یک مسیر مشترک استفاده می‌کنند و حتی در حالت
  بازیابی Task، کاربر را به Home برمی‌گردانند نه Launcher.

## Performance، حریم خصوصی و پایداری

- فقط اسلاید فعال Compose می‌شود؛ پس‌زمینه عریض حداکثر 1280×720 و پوستر
  حداکثر 420×630 درخواست می‌شود.
- تکمیل عمومی Metadata فقط هنگام ورود واقعی به Spotlight انجام می‌شود، نه
  هنگام چرخش Hero.
- Cookie، Token، Password، DRM یا آدرس فایل پخش به منبع عمومی ارسال نمی‌شود؛
  فقط عنوان، سال، نوع محتوا و شناسه عمومی در صورت وجود.
- تست‌های واحد، Lint، ساخت APK و QA واقعی 1920×1080 برای فیلم و سریال ایرانی
  و خارجی موفق بودند و کرش ثبت‌شده صفر بود.

versionCode: 34

## تکمیل Candidate نهایی

- سال‌ها بدون برچسب اضافه «شمسی/میلادی» نمایش داده می‌شوند.
- کشور از Hero حذف و به صفحه تصمیم‌گیری Spotlight محدود شد.
- فلش‌های قبلی/بعدی Hero اکنون پایینِ چپ و خارج از مسیر متن هستند.
- صفحه توضیحات کشور، زبان، ژانر، مدت، دوبله/زیرنویس، خلاصه طولانی‌تر و عوامل
  بیشتری را در یک نمای فشرده و TV-friendly نشان می‌دهد.
- Rating خارجی با چهار سطح رنگی کیفیت IMDb نمایش داده می‌شود.
- اطلاعات کارگردان و بازیگران عنوان‌های ایرانی ناقص، پس از تطبیق دقیق، از
  صفحه عمومی رسمی شیدا تکمیل می‌شود؛ تست واقعی «بامداد خمار» موفق بود.
- دسترسی موقت QA به Spotlight در نسخه نهایی حذف شد و Activity دوباره
  `exported=false` است.
