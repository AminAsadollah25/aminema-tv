# Aminema 0.16.0

## اصلاح — پوسترها بریده و بزرگ‌شده نشان داده می‌شدند

هر عنوان در سرویس ایرانی **دو تصویر جدا** دارد و این دو جای هم استفاده
نمی‌شوند:

| فیلد | اندازه واقعی (اندازه‌گیری‌شده) | شکل |
|---|---|---|
| `thumbnail` | ۱۳۵۰×۱۶۸۷ ، ۱۰۸۰×۱۹۲۰ | عمودی |
| `cover` | ۱۲۰۰×۶۸۰ ، ۲۵۵۹×۱۱۹۳ | عریض |

کد در چهار جا **تصویر عریض را اول برمی‌داشت** و داخل کارت پوستر عمودی
می‌گذاشت، که یعنی از یک عکس افقی فقط یک نوار وسطش باقی می‌ماند. برای همین
پوستری مثل «کلاغ» با متن عوامل نصفه و دو طرف بریده دیده می‌شد. حالا برای
کارت عمودی تصویر عمودی برداشته می‌شود، و تصویر عریض در فیلد تازه‌ای نگه
داشته می‌شود تا جایی استفاده شود که واقعاً به آن می‌خورد.

## بازطراحی Hero

- **پوستر تمام‌قد**، چسبیده به گوشه راست، کامل و بدون بریدگی.
- **رنگ Hero از خودِ فیلم گرفته می‌شود.** همان تصویر ریزی که برای محو‌کردن
  پس‌زمینه گرفته می‌شود یک بار هم پیکسل‌به‌پیکسل خوانده می‌شود. رنگ نهایی
  ترکیبی است از رنگ کلی پوستر و رنگی‌ترین بخشش — چون فقط «رنگی‌ترین» بخش،
  یک پوستر کِرِم با تیتر قرمز را قرمز خون نشان می‌داد.
- **لبه‌ی پوستر دیگر خط ندارد.** رنگ کارت هرچه به پوستر نزدیک‌تر می‌شود به
  رنگ خود پوستر نزدیک می‌شود، و فقط نوار بیرونی پوستر نرم می‌شود. محو‌کردن
  عمیقِ خود پوستر امتحان شد و کنار گذاشته شد چون پوستر را ناخوانا می‌کرد.
- **اسکلت ثابت.** عنوان، متادیتا و خلاصه هرکدام جای ثابت خودشان را دارند
  حتی وقتی خالی‌اند، چون سرویس‌ها اطلاعات را خیلی نابرابر می‌دهند و قبلاً
  دکمه‌ها در هر اسلاید یک ارتفاع می‌نشستند.
- سالِ داخل عنوان (مثل `House of the Dragon (2022)`) دیگر با چیپ سال تکرار
  نمی‌شود.

## اصلاح — دکمه‌ها بریده می‌شدند

در Hero و در صفحه Spotlight، دکمه‌ی اصلی («ادامه تماشا») آخرین عنصر یک ستون
با ارتفاع محدود بود، پس وقتی محتوای بالای آن زیاد می‌شد، فشرده و بریده
می‌شد. حالا بخش توصیفی جا می‌دهد، نه دکمه.

## اصلاح — چرخش Hero پس‌زمینه را می‌دزدید

وقتی روی ردیف‌های پایین حرکت می‌کردید، Hero هر ۱۱ ثانیه پس‌زمینه را عوض
می‌کرد. حالا تا وقتی روی ردیف‌ها هستید دست نمی‌زند.

## جدید — برگزیده‌های خود سایت‌ها

دو ردیف تازه پایین‌تر از فهرست‌ها، با **هنر عریض خودِ سایت‌ها** (همان
بنرهایی که در صفحه اصلی‌شان می‌چرخانند): «برگزیده‌های سینمای ایران» و
«برگزیده‌های سینمای جهان». این‌ها انتخاب سرمقاله‌ای خود سرویس‌اند، نه
مرتب‌شده بر اساس تازگی.

کارت‌ها ۱۶:۹ هستند و اطلاعات **روی** تصویر می‌نشیند، چون نوار متن زیر یک
تصویر افقی هر کارت را دو برابر بلند می‌کرد.

هزینه‌ی شبکه: سرویس ایرانی صفر — این داده در همان پاسخی بود که از قبل
می‌گرفتیم. سرویس خارجی یک بازدید صفحه اضافه، نه به‌ازای هر عنوان.

## پایداری بیلد

پوشه‌ی خروجی بیلد به بیرون از مسیر همگام‌شونده با iCloud منتقل شد. iCloud
وسط بیلد فایل‌های تکراری می‌ساخت (`BuildConfig 2.class`, `banner 2.png`) و
باعث می‌شد Resource parser و R8 بشکنند. خروجی حالا در
`~/Library/Caches/AminemaBuild/` ساخته می‌شود. این فقط محیط توسعه را عوض
می‌کند، نه خود برنامه را.

کد نسخه `33`، شناسه و امضا بدون تغییر.

---
versionCode: 33
