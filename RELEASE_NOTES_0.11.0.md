# Aminema 0.11.0

## اصلاح — پخش مستقیم گاه‌به‌گاه کار نمی‌کرد

وقتی ParsiFlix با ناوبری داخلی (بدون لود کامل صفحه) به صفحه فیلم می‌رسید،
تابع پخش مستقیم اصلاً صدا زده نمی‌شد — فقط تابع «ادامه تماشا» فراخوانی
می‌شد. حالا هر دو مسیر ورود به صفحه محتوا، پخش مستقیم را امتحان می‌کنند.
پنجره تلاش مجدد هم یک مرحله (تا ۴.۵ ثانیه) طولانی‌تر شد برای وقتی سایت
کندتر لود می‌شود.

## اصلاح — ادامه تماشا از دستگاه دیگر گاهی جا می‌ماند

Sync accounts خودش سقف ۱۲ آیتم روی فهرست هر سایت می‌گذاشت، در حالی که لایه
ادغام داخلی تا ۳۰ آیتم را می‌پذیرد. این سقف به ۳۰ رسید. برای فیلم خارجی هم،
پیدا کردن بخش «مشاهدات اخیر» فقط یک بار با تأخیر ثابت امتحان می‌شد؛ حالا اگر
آن بخش کندتر لود شود چند بار دیگر هم تلاش می‌کند.

## جدید — پخش زنده، دم‌دست

دکمه **پخش زنده** کنار جستجو در بالای Home اضافه شد. با یک کلیک فهرست
واقعی کانال‌های سرویس ایرانی (با لوگو) باز می‌شود؛ انتخاب هر کانال همان‌جا
پخش را عوض می‌کند، و تمام‌صفحه‌شدن از همان دکمه استاندارد پخش‌کننده در
دسترس است.

## جدید — یوتیوب فارسی

ردیف **یوتیوب فارسی** پایین‌تر در Home اضافه شد — عمداً کم‌اهمیت‌تر و
پایین‌تر از بخش‌های اصلی.

کد نسخه `24`، شناسه و امضا بدون تغییر.

---
versionCode: 24
