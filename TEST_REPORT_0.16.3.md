# گزارش تست Aminema 0.16.3

تاریخ: 2026-08-08
محیط: AVD `Television_1080p`، حساب واقعی لاگین‌شده، نصب درجا با `adb install -r`

## تست‌های خودکار

- `:app:testDebugUnitTest`: موفق
- `:app:lintDebug`: موفق
- `:app:assembleDebug`: موفق
- بررسی APK برای نبودن `ProviderDomProbe/AminSafeProbe`: موفق
- Fatal Exception پس از اجرا: مشاهده نشد

## تأیید دستی مالک محصول

| سناریو | نتیجه |
|---|---|
| لیسانسه‌ها، فصل ۱، انتخاب قسمت | قسمت انتخابی درست پخش شد |
| لیسانسه‌ها، فصل ۲، انتخاب قسمت | قسمت انتخابی درست پخش شد |
| بامداد خمار، فصل ۲ قسمت ۴ | فصل و قسمت درست پخش شد |
| Life, Larry… در FilmRooz | مسیر انتخاب و پخش درست عمل کرد |
| پوستر لیسانسه‌ها | پوستر عمودی درست نمایش داده شد |
| پوسترهای عناوین تست‌شده | درست نمایش داده شدند |

## ریشهٔ باگ‌های رفع‌شده

1. Direct Play عمومی هم‌زمان با Episode State Machine اجرا و دکمهٔ Continue سایت را فعال می‌کرد.
2. ParsiFlix یک MouseEvent و یک `click()` دریافت می‌کرد؛ اکنون فقط یک Click دارد.
3. فصل از روی Episode ID تکراری حدس زده می‌شد؛ اکنون `Season` صریح منتقل می‌شود.
4. FilmRooz لینک `.eDbox` را به‌عنوان پخش می‌گرفت؛ اکنون فقط `.eSbox` معتبر است.
5. پوستر افقی Recent بر Thumbnail عمودی کاتالوگ غلبه می‌کرد؛ ترتیب Merge اصلاح شد.

## محدودیت باقی‌مانده

- Multi-editionهای نادر مانند Normal/Uncut در نسخهٔ بعدی به انتخابگر مستقل نیاز دارند.
- موفقیت امولاتور جای تست نهایی روی Android Box فیزیکی را نمی‌گیرد.
