# Aminema 0.18.5.1 — Cinematic Performance & TV Polish

## تغییرات

- اینترو حذف نشده است؛ هم‌زمان با پخش اینترو، آماده‌سازی اولیهٔ کاتالوگ در پس‌زمینه
  شروع می‌شود تا بعد از ورود به Home انتظار کمتری باقی بماند.
- فونت Vazirmatn به‌صورت داخلی اضافه شد تا فارسی و عنوان‌های لاتین روی Android TV
  شکل و فاصلهٔ ثابت‌تری داشته باشند.
- کارت‌ها، Spotlight، کیبورد جستجو و کنترل‌های Live TV ظاهر Liquid Glass سبک گرفتند:
  شفافیت کنترل‌شده، خط نور ظریف و بدون Blur سنگین برای هر کارت.
- حرکت Focus ریموت و تغییر پس‌زمینه کوتاه‌تر و آرام‌تر شد تا جابه‌جایی بین ردیف‌ها
  سریع‌تر و کم‌صداتر باشد.
- Health Scan پخش زنده در سطح Application باقی می‌ماند؛ خارج‌شدن از صفحهٔ Live TV
  اسکن پس‌زمینه را قطع نمی‌کند و تب‌های «فعال» و «همه کانال‌ها» حفظ شده‌اند.
- تطبیق فصل/قسمت در Provider مقاوم‌تر شد و اطلاعات شمارهٔ قسمت را از دادهٔ صریح
  صفحه در اولویت می‌گیرد؛ fallback اشتباهِ ایندکسی اضافه نشد.
- پنجرهٔ ادغام Home محدود و سبک نگه داشته شد؛ View All همچنان می‌تواند از کش ادامه دهد.

## سازگاری و امنیت

- `applicationId`، امضای کانال Debug، Login، Cookie، WebStorage، Continue Watching،
  Favorites و دادهٔ محلی تغییر نکرده‌اند.
- هیچ لینک مدیا، توکن، DRM یا محتوای محافظت‌شده استخراج یا ذخیره نمی‌شود.
- APK قابل نصب این انتشار، Debug-signed است تا روی نصب فعلی تلویزیون با `adb install -r`
  یا نصب دستی درجا به‌روزرسانی شود. بیلد Release جداگانه ساخته شد اما چون keystore
  انتشار در پروژه تنظیم نشده، unsigned است و Asset انتشار نیست.

## تست

- Unit Test، Lint و `assembleDebug` موفق.
- `assembleRelease` نیز موفق است؛ خروجی unsigned است.
- APK نسخهٔ 0.18.5.1 با `versionCode 47` روی امولاتور 1920×1080 با نصب درجا اجرا شد.
- داده‌ها و نشست‌های امولاتور پاک نشدند و Fatal Exception در بررسی پس از اجرا دیده نشد.

versionCode: 47
