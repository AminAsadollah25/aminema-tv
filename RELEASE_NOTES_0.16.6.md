# Aminema 0.16.6 — کتابخانه یکپارچه و انتخاب منبع

این نسخه زیرساخت امن اضافه‌شدن آرشیو سوم را می‌سازد؛ بدون اینکه یک فیلم یا
سریال مشترک چند بار در نتایج جستجو نمایش داده شود.

## تغییرات اصلی

- نتایج ParsiFlix و FilmRooz در یک ردیف مشترک نمایش داده می‌شوند.
- عنوان‌های مشترک فقط وقتی با شواهد قابل‌اعتماد یکی باشند، به یک کارت تبدیل
  می‌شوند: شناسه IMDb، یا عنوان+سال+نوع، یا عنوان+نوع همراه تطابق معتبر عوامل.
- عنوان مشابه به‌تنهایی برای ادغام کافی نیست؛ نتیجه مشکوک جدا باقی می‌ماند.
- کارت مشترک تعداد منابع در دسترس را نشان می‌دهد.
- در صفحه توضیحات می‌توان بین منبع‌های معتبر همان عنوان جابه‌جا شد.
- نام واقعی منبع، `ParsiFlix` و `FilmRooz`، در انتخابگر منبع دیده می‌شود.
- شکست یکی از آرشیوها نتیجه‌های آرشیو سالم را حذف یا مسدود نمی‌کند.
- نوشتار فارسی/عربی، نیم‌فاصله، فاصله و شکل‌های `spiderman`، `spider man` و
  `spider-man` برای تطبیق داخلی نرمال می‌شوند.
- «لیسانسه‌ها» با دو صفحه واقعی ParsiFlix و FilmRooz به‌عنوان نمونه پذیرش
  یک عنوان/یک کارت/دو منبع استفاده شد.

## مرز این نسخه

- هنوز آداپتر سایت سوم اضافه نشده است؛ این نسخه پایه لازم برای آن است.
- Continue Watching، علاقه‌مندی‌ها و Recently Opened هنوز به شناسه Canonical
  مهاجرت نکرده‌اند. برای حفظ دیتای فعلی، این انتقال در یک مرحله جدا و
  برگشت‌پذیر انجام می‌شود.
- هیچ Cookie، Login، Local storage، Library یا App data پاک یا بازنویسی نشد.
- فقط URL عادی صفحه عنوان و Metadata قابل‌مشاهده استفاده می‌شود؛ هیچ لینک
  مدیا، Token، Manifest یا DRM خوانده و ذخیره نمی‌شود.

versionCode: 39
