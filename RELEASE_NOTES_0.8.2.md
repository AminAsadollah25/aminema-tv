# Aminema 0.8.2

نسخه 0.8.2 کلیک روی کارت **فیلم** را یک قدم جلوتر می‌برد: به‌جای توقف روی
صفحه جزئیات، همان صفحه پخشِ خود سایت باز می‌شود.

## رفتار جدید

**فیلم خارجی**

- بین گزینه‌های پخش آنلاینِ خود صفحه، بهترین را انتخاب می‌کند:
  - اگر **دوبله فارسی** داشته باشد، دوبله مقدم است
  - کیفیت: **۱۰۸۰ ← ۷۲۰ ← ۴۸۰**
  - **۲۱۶۰ هیچ‌وقت** انتخاب نمی‌شود
- انتخاب به برچسب منبع (WEBRip، DVDRip، BluRay و…) وابسته نیست؛ فقط عدد
  کیفیت و کلید زبان خوانده می‌شود
- اگر نسخه‌ای بدون برچسب کیفیت باشد (فیلم‌های تک‌نسخه‌ای) همان باز می‌شود
- اگر فیلم اصلاً پخش آنلاین نداشته باشد، دقیقاً مثل قبل صفحه جزئیات باز
  می‌ماند

**فیلم ایرانی**

- آن‌جا فهرست کیفیت وجود ندارد و صفحه یک دکمه تماشا دارد؛ پس همان دکمه‌ی
  خودِ سایت زده می‌شود

**سریال‌ها فعلاً بدون تغییرند** و روی صفحه جزئیات باز می‌شوند. انتخاب
خودکار قسمت در نسخه بعدی می‌آید.

## امنیت

این قابلیت از خط قرمزها عبور نمی‌کند:

- مقصد یک **صفحه عادی خود سایت** است (`/stream/...`) — همان صفحه‌ای که
  Continue Watching از قبل باز می‌کرد
- هیچ لینک فایل مدیا، لینک امضاشده، DRM یا Token خوانده، ذخیره یا Log
  نمی‌شود
- کاری که انجام می‌شود دقیقاً همان کلیکی است که خودتان روی سایت می‌زدید،
  داخل WebView و با حساب خودتان

## تنظیمات

رفتار از `services.json` می‌آید و قابل تغییر است:

```json
"directPlay": {
  "preferredLanguages": ["dubbed-fa", "original"],
  "preferredHeights": [1080, 720, 480]
}
```

نصب‌های قبلی این تنظیم را به‌صورت خودکار دریافت می‌کنند، ولی اگر خودتان
مقدارش را عوض کرده باشید دست‌نخورده می‌ماند.

## بروزرسانی

- شناسه برنامه بدون تغییر: `com.amin.tvos.debug`
- کد نسخه: `17`
- امضا مثل نسخه‌های قبل؛ مستقیم Update می‌شود و داده‌ای پاک نمی‌شود.
