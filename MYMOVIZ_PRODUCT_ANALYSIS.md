# تحلیل محصول و منبع سوم MyMoviz برای Aminema

تاریخ بررسی: 2026-07-29  
صفحه بررسی‌شده: `https://mymoviz.co/_modern/home`

این بررسی فقط روی رابط عادی و لاگین‌شده سایت، لینک صفحه عنوان، اطلاعات
قابل‌مشاهده و مسیر عادی Watch انجام شده است. هیچ Cookie، Token، Password،
DRM value یا Media/Stream URL بخشی از این گزارش یا معماری پیشنهادی نیست.

## نتیجه اجرایی

MyMoviz فقط «یک سرویس سوم» نیست؛ از دید محصول سه مزیت مهم دارد:

1. آرشیو مکمل، به‌خصوص برای نسخه‌های دوبله فارسی.
2. شناسه IMDb در صفحه عنوان که برای حذف تکراری‌ها بسیار قابل‌اعتماد است.
3. مدل کامل سریال شامل فصل، قسمت، آخرین انتشار، پیشرفت تماشا، قسمت بعدی،
   دنبال‌کردن سریال و تقویم پخش.

به همین دلیل نباید مستقیم یک Rail سوم به Home اضافه شود. ابتدا باید Aminema
از مدل «هر کارت متعلق به یک سایت است» به مدل «یک عنوان، چند نسخه و چند منبع»
مهاجرت کند.

## مشاهدات Home

- Hero چرخشی با عنوان، سال، امتیاز، نوع، کیفیت، دوبله/زیرنویس و خلاصه.
- Continue Watching با Progress و لینک عادی Watch.
- بخش‌های مستقل آخرین فیلم‌ها، سریال‌های به‌روز شده و تازه‌ترین دوبله‌ها.
- نمایش صریح `فصل X قسمت Y` روی کارت سریال.
- فیلتر و Search پیشرفته بر اساس نوع، امتیاز، سال، فقط دوبله، وضعیت پخش،
  کیفیت، ژانر، کشور، شبکه و زبان زیرنویس.
- امکان افزودن نتیجه یک Search یا Filmography یک شخص به Home.
- امکان سفارشی‌سازی Feed و حذف دسته‌های ناخواسته.
- صفحه Person با Filmography و نقش‌های مختلف.
- صفحه شخصی با Continue، My List، قسمت بعدی و تقویم پخش.
- در زمان بررسی، آمار خود سایت 36,661 عنوان و 9,954 عنوان دوبله را نشان می‌داد:
  8,937 فیلم دوبله و 1,017 سریال دوبله.

## قابلیت‌هایی که ارزش اقتباس دارند

### 1. Hero کاربردی، نه تزئینی

Hero در Aminema باید تصمیم را سریع کند: یک تصویر سینمایی، عنوان، سال،
دوبله/زیرنویس، یک خلاصه بدون اسپویل و فقط دو Action:

- `تماشا / ادامه`
- `جزئیات`

Download، Comment، Rating input و Trailer در مسیر اصلی Aminema جایی ندارند.

### 2. «فصل/قسمت» به‌عنوان داده اصلی

آخرین انتشار نباید داخل خلاصه گم شود. روی کارت سریال یک Badge مستقل
`فصل ۳ • قسمت ۶` لازم است. Spotlight نیز باید سه Action صادقانه داشته باشد:

- `ادامه قسمت بعد`
- `آخرین قسمت منتشرشده`
- `انتخاب فصل و قسمت`

### 3. فید قابل سنجاق‌شدن

ایده `افزودن به خانه` بسیار ارزشمند است، ولی برای TV باید ساده‌تر شود:

- از صفحه Search، Person، Genre یا Geek Collection گزینه `سنجاق به خانه`
- حداکثر چند Rail فعال و قابل مرتب‌سازی
- بدون فرم پیچیده روی Home

### 4. صفحه شخصی واقعی

به‌جای پراکندگی Continue، Favorites و Series، یک مقصد `برای من` ساخته شود:

- ادامه تماشا
- قسمت بعدی
- سریال‌های دنبال‌شده
- فهرست من
- اعلان‌های کم‌تعداد و مهم

## چیزهایی که نباید کپی شوند

- تراکم زیاد نسخه Desktop و متن‌های ریز
- رنگ روشن و سفید به‌جای فضای سینمایی تاریک
- نمایش هم‌زمان Download، Comment، Rating، Notification و ده‌ها Metadata
- Railهای زیاد بدون اولویت
- دکمه‌های کوچک و Focus نامشخص برای D-pad

Aminema باید قدرت داده‌ای MyMoviz را بگیرد، اما UI آن را به تجربه کم‌اصطکاک
Apple TV/Netflix تبدیل کند.

## جستجو و مشکل Spider-Man

تست واقعی:

- `spiderman` → صفر نتیجه
- `spider man` → 16 نتیجه
- `spider-man` → 13 نتیجه

بنابراین Unified Search باید برای هر Query چند Variant امن بسازد:

- متن اصلی
- حذف یا افزودن فاصله
- تبدیل فاصله و خط تیره به یکدیگر
- حذف Apostrophe و علائم غیرضروری
- یکسان‌سازی `ي/ی` و `ك/ک`
- حذف نیم‌فاصله و فاصله‌های تکراری

هر Provider Variant مناسب خود را دریافت می‌کند و نتایج بعداً بر اساس هویت
Canonical ادغام می‌شوند. این کار فقط روی عبارت جستجو انجام می‌شود و Scrape
محافظت‌شده یا حدس‌زدن URL رسانه نیست.

## معماری حذف تکراری

### مدل داده

```text
CanonicalMedia
  id
  imdbId?
  normalizedTitle
  localizedTitles[]
  year?
  type
  metadata
  variants[]

SourceVariant
  providerId
  detailPageUrl
  normalWatchPageUrl?
  hasPersianDub
  hasPersianSubtitle
  latestSeason?
  latestEpisode?
  availability
  lastVerifiedAt
```

### سطح اطمینان Match

1. **قطعی:** IMDb ID یکسان + نوع یکسان.
2. **قوی:** عنوان Normalizeشده + سال + نوع.
3. **متوسط:** عنوان فارسی/انگلیسی هم‌ارز + سال نزدیک + نوع.
4. **مبهم:** فقط شباهت عنوان؛ این مورد خودکار Merge نمی‌شود.

### قانون UI

- یک فیلم یا سریال فقط یک کارت دارد.
- کارت Provider brand را فریاد نمی‌زند.
- Spotlight یک بخش کوچک `نسخه‌های موجود` دارد.
- اگر دوبله اولویت کاربر باشد، نسخه دوبله به‌صورت پیش‌فرض انتخاب می‌شود.
- کاربر همیشه می‌تواند نسخه زبان اصلی یا Provider دیگر را انتخاب کند.
- Aminema هرگز وسط پخش، منبع را بی‌اجازه عوض نمی‌کند.

## آزمون قبل از Provider سوم

ابتدا Dedupe فقط روی ParsiFlix و FilmRooz فعال می‌شود. Query پذیرش اولیه
`لیسانسه` است، چون هر دو Provider همان سریال را برمی‌گردانند.

Acceptance:

- یک کارت برای یک اثر
- هر دو SourceVariant داخل همان کارت
- Continue/Favorite/Recent به CanonicalMedia وصل باشند، نه Provider card
- انتخاب نسخه دوبله یا زبان اصلی قابل پیش‌بینی باشد
- Match مبهم جدا بماند و اشتباه Merge نشود

بعد از آن یک Coverage Lab برای MyMoviz اجرا می‌شود:

- نمونه حداقل 100 عنوان فیلم و سریال
- درصد Match قطعی با IMDb
- درصد Match عنوان/سال
- عنوان‌های فقط MyMoviz
- مزیت واقعی دوبله
- سرعت Catalog/Detail/Watch route
- پایداری Login و Session در WebView

عدد هم‌پوشانی عمومی فقط بعد از این گزارش اعلام می‌شود؛ مشاهده فعلی بالا بودن
هم‌پوشانی را تأیید می‌کند، اما برای عدد دقیق کافی نیست.

## سریال‌های دنبال‌شده

### راه سریع و مقاوم

- Follow دستی سریال
- Baseline دستی: `تا فصل X قسمت Y دیدم` یا `به‌روز هستم`
- مقایسه با آخرین انتشار Catalog
- عبارت UI: `۲ قسمت جدید از آخرین وضعیت ثبت‌شده`

### راه بهتر

- خواندن Progress/Checkmark عادی و قابل‌مشاهده حساب Provider
- ترکیب آن با Playback واقعی Aminema
- نگهداری Evidence جدا برای هر Provider و هر فصل/قسمت
- نمایش `دیده‌نشده` فقط وقتی شاهد دقیق وجود دارد

### یافته مهم

MyMoviz همین حالا روی صفحه سریال Progress، فصل‌ها، تعداد قسمت، `قسمت بعدی شما`
و علامت‌گذاری قسمت/فصل را ارائه می‌کند. صفحه `من` نیز برای سریال دنبال‌شده
قسمت بعدی و تقویم پخش دارد. بنابراین برای این Provider مسیر دقیق وجود دارد.

FilmRooz در نشست‌های قبلی Checkmark را Device-local نشان داده است؛ در نتیجه
برای Cross-device باید Baseline دستی و Playback محلی همچنان Fallback رسمی
بماند. ParsiFlix نیز تا زمان Probe دقیق نباید مبنای ادعای «ندیده» قرار گیرد.

## بنرهای سه Provider

هر سه سایت یک سطح Promo/Carousel قابل‌استفاده دارند. مدل پیشنهادی:

```text
PromoBanner
  id
  title
  artwork
  destinationType = TITLE | LIVE | PROVIDER_PAGE
  canonicalMediaId?
  providerId
  normalPageUrl
  expiresAt?
```

در Home یک Hero واحد با نام محصولی مثل `روی پرده` یا `پیشنهاد امشب` نمایش
داده می‌شود:

- چرخش آرام 8 تا 10 ثانیه
- توقف هنگام Focus یا Hover
- بدون دزدیدن Focus
- Banner عنوان → Spotlight
- Banner پخش زنده → اجرای مستقیم با قانون یک‌کلیکی فعلی
- Banner ناشناخته → صفحه عادی Provider
- حذف Bannerهای تکراری با Canonical ID

## مسیر Provider سوم

1. **Adapter Read-only:** Catalog، Search، Detail metadata و Login state.
2. **Coverage Gap:** فقط عنوان‌هایی که در دو منبع فعلی نیستند یا نسخه دوبله
   بهتری دارند وارد UI شوند.
3. **Normal Watch:** فقط مسیر Watch عادی سایت؛ بدون بررسی URL رسانه.
4. **Account Sync:** Continue و Series progress عادی حساب.
5. **Best Source Resolver:** انتخاب نسخه بر اساس دوبله، سلامت Provider و
   تنظیم کاربر.

## منبع ایرانی آینده

منبع چهارم ایرانی نباید معماری تازه‌ای بسازد. همان قرارداد
`CatalogProvider + CanonicalMedia + SourceVariant` را پیاده می‌کند. قبل از
ورود به Home باید Dedupe و Coverage Gap را پاس کند.

## Geek Mode

Geek Mode یک لایه Collection روی CanonicalMedia است، نه مجموعه لینک سایت:

```text
CuratedCollection
  id
  title
  orderMode
  entries[canonicalId]
  manifestVersion
```

نمونه:

- MCU — ترتیب اکران / ترتیب زمانی / همه آثار
- Star Wars
- The Lord of the Rings
- Harry Potter
- فیلم‌شناسی کارگردان یا بازیگر

قابلیت‌ها:

- `افزودن به صفحه اصلی`
- `از اول ببین`
- `خفن‌ها را ببین`
- مخفی‌کردن یا تیک‌زدن موارد دیده‌شده
- Resolve خودکار بهترین SourceVariant موجود

لیست‌های تقریباً ثابت به‌صورت JSON نسخه‌بندی‌شده همراه اپ نگهداری می‌شوند؛
فقط هنگام اضافه‌شدن اثر جدید Manifest به‌روزرسانی می‌شود.

## اصول دائمی تصمیم محصول

برای هر Feature قبل از اجرا چهار خروجی لازم است:

1. مسئله واقعی کاربر
2. راه سریع و مقاوم
3. راه کامل و بلندمدت
4. بهبود مکمل UI/UX برای رساندن کاربر به تماشا با کلیک کمتر

اگر راه دقیق قابل‌اعتماد نبود، Aminema باید راه جایگزین صادقانه ارائه کند؛
نباید داده حدس بزند یا Feature را صرفاً به‌دلیل نبود API کنار بگذارد.
