# Aminema 0.18.2 — Infinite Catalog Continuation Fix

این نسخه مشکل تکرار/ایستادن دکمهٔ «نمایش بیشتر» در کتابخانهٔ فیلم و سریال را
حل می‌کند.

## تغییرات

- اتصال صفحهٔ کتابخانه به دادهٔ زندهٔ کاتالوگ، نه Snapshot قدیمی Home.
- دریافت تدریجی چهار صفحهٔ بعدی MyMoviz در هر درخواست.
- شروع واکشی از بعدِ آخرین صفحهٔ ذخیره‌شده.
- ثبت تعداد صفحات دریافت‌شده و مهاجرت امن کش نسخه‌های قبلی.
- حفظ ادغام و حذف تکراری‌های FilmRooz/MyMoviz.

## تست

- `testDebugUnitTest`: موفق
- `lintDebug`: موفق
- `assembleDebug`: موفق
- نصب درجا روی امولاتور 1920×1080: موفق
- رشد مشاهده‌شدهٔ سریال خارجی: ۲۰۸ → ۴۴۸ عنوان
- کش MyMoviz در تست: ۴۴۰ سریال
- Login، Cookie، WebStorage، Continue و Favorite پاک نشده‌اند.

## محدودیت شناخته‌شده

فهرست تا زمانی ادامه پیدا می‌کند که خود آرشیو عمومی ارائه‌دهنده صفحهٔ جدید
داشته باشد یا سقف داخلی ۲۰۰ صفحه برسد.
