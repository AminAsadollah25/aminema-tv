# Aminema 0.12.1

## Continue Watching قابل‌اعتمادتر و مشترک بین دستگاه‌ها

- Aminema در شروع سرد برنامه، حداکثر هر ۱۵ دقیقه، Continue/Recent حساب
  لاگین‌شدهٔ هر دو سرویس را تازه می‌کند؛ بنابراین تلویزیون و امولاتور بعد
  از Sync به یک فهرست مشترک می‌رسند.
- نتیجه موفق حساب، مرجع عضویت ردیف Continue است. اطلاعات محلی فقط Player
  page و زمان دقیق همان عنوان مشترک را کامل می‌کند؛ موارد قدیمیِ خارج‌شده
  از حساب بی‌دلیل در یک دستگاه باقی نمی‌مانند.
- دکمه دستی **Sync accounts** همچنان برای تازه‌سازی فوری موجود است.

## رفع باگ «کلیک شد ولی پلیر باز نشد»

- Aminema دیگر اجرای `click()` روی دکمه سایت را موفقیت حساب نمی‌کند.
- Retryهای زمان‌بندی‌شده تا وقتی ادامه دارند که واقعاً مسیر Player تأیید
  شود یا یک رویداد واقعی HTML5 Video برسد.
- انتخاب دکمه، کنترل Visible/Enabled و امتیازدهی دقیق‌تری دارد تا Container
  یا دکمه نامرتبط انتخاب نشود.
- Player URLهایی که درواقع همان Detail URL هستند دیگر به‌عنوان مقصد قابل
  Resume اعتماد نمی‌شوند؛ برای فیلم‌های واجد شرایط Resolver عادی کیفیت و
  زبان سایت دوباره اجرا می‌شود.

## انتقال سینمایی به پخش

در فاصله اجتناب‌ناپذیر بارگذاری Detail و آماده‌شدن کنترل‌های سایت، صفحه
جزئیات دیگر چشمک نمی‌زند. یک لایه سینمایی با تصویر اختصاصی مسکات Aminema
نمایش داده می‌شود:

**«پاپ‌کورن یادت نره… 🍿»**  
**«داریم فیلمت رو از همون‌جایی که جا گذاشتی میاریم؛ خوش بگذره!»**

اگر سایت ظرف ۱۴ ثانیه مقصد قابل پخش ارائه نکند، Detail page به‌صورت امن
برای انتخاب دستی نمایش داده می‌شود. Back در زمان Loading فوراً به Home
برمی‌گردد.

## فلش‌های مشترک ردیف‌ها

- دو فلش چپ/راست در گوشه راست تمام ردیف‌های افقی Home و Search اضافه شد.
- کلیک موس یا OK کنترل، تقریباً یک صفحه از نتایج را با انیمیشن نرم جابه‌جا
  می‌کند.
- فلش در ابتدا/انتهای ردیف کم‌رنگ و غیرفعال می‌شود.
- متن اضافی «پخش مستقیم • تمام‌صفحه» از کنار عنوان پخش زنده حذف شد.

## حریم و امنیت

همه مسیرها همچنان صفحه عادی سایت هستند. هیچ Media URL، Stream request،
Cookie، Token، Header احراز هویت یا DRM خوانده، ذخیره یا Log نمی‌شود.

---
versionCode: 26
