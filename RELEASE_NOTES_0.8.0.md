# Aminema 0.8.0

نسخه 0.8.0 دو ردیف تازه به Home اضافه می‌کند: **تازه‌های ایرانی** و
**تازه‌های خارجی**. اینترو، Login، Cookies، سرویس‌ها، کیبورد، Continue
Watching، Favorites و مرورگر بدون تغییر مانده‌اند.

## تغییرات

- دو بخش Native جدید در Home با انتخاب‌گر `همه | فیلم | سریال`
- انتخاب هر ردیف ذخیره می‌شود و بعد از راه‌اندازی دوباره حفظ می‌ماند
- نمایش عنوان، پوستر و نوع فیلم/سریال روی هر کارت
- کلیک هر کارت فقط **صفحه معمولی جزئیات** همان محتوا را در مرورگر باز می‌کند
- دکمه **بروزرسانی** و نمایش زمان آخرین بروزرسانی روی هر ردیف
- کش سبک محلی: Home بلافاصله از کش باز می‌شود و فقط بروزرسانی دستی به شبکه
  می‌رود
- وضعیت خالی و وضعیت خطای تلویزیونی برای هر ردیف
- Adapter هر سرویس مستقل است؛ خرابی یکی، ردیف دیگر و کش قبلی را از بین نمی‌برد

## منابع داده (همه از حساب لاگین‌شده خودتان)

**تازه‌های ایرانی**

- `همه`: همان بخش «جدیدترین‌ها»ی صفحه اصلی حساب
- `فیلم` و `سریال`: همان درخواستی که خود سایت برای صفحه‌های
  `/medias/movies` و `/medias/series` می‌زند

**تازه‌های خارجی**

- `فیلم`: «فیلم های جدید» از منوی خود حساب
- `سریال`: «سریال های جدید» از منوی خود حساب
- `همه`: ترکیب دو فهرست بالا، چون خود سایت صفحه «جدیدهای ترکیبی» ندارد

مسیر بخش سریال خارجی حدس زده نشده و مستقیماً از منوی حساب لاگین‌شده خوانده
و تأیید شده است.

## امنیت

- هیچ Media URL، Stream URL، DRM value، Token یا Password خوانده یا ذخیره
  نمی‌شود
- فقط عنوان، پوستر و لینک پایدار صفحه جزئیات ذخیره می‌شود
- توکن و کوکی داخل WebView باقی می‌مانند
- پوستر نیازمند ورود فقط وقتی با کوکی درخواست می‌شود که روی همان دامنه سرویس
  باشد
- نام ارائه‌دهنده‌ها روی Home نمایش داده نمی‌شود؛ فقط «ایرانی» و «خارجی»

## بروزرسانی و حفظ اطلاعات

- شناسه برنامه بدون تغییر: `com.amin.tvos.debug`
- کد نسخه جدید: `15`
- گواهی امضای APK با نسخه‌های 0.7.3 و 0.7.4 یکسان است، پس روی برنامه
  نصب‌شده Update می‌شود.
- کوکی‌ها، ورودها، تنظیمات، Continue Watching، Favorites و History پاک
  نمی‌شوند.

نسخه قبلی را Uninstall نکنید؛ فقط گزینه `Update` را بزنید.
