# نقشه توسعه Aminema

فهرست ایده‌های خارج از صف قطعی و دروازه‌های تحقیق در `IDEA_BACKLOG.md`
نگهداری می‌شود. ایده تازه ابتدا آنجا ثبت می‌شود و فقط بعد از Research و تصمیم
معماری وارد صف نسخه‌ها خواهد شد.

یافته‌های تصویری و ماشین حالت فصل/قسمت FilmRooz در
`SERIES_LAB_FILMROOZ.md` ثبت شده است.

نسخه جاری `0.16.4 / code 36 — In-app Update Banner Hotfix` است. نتیجهٔ چک
دستی Settings اکنون مستقیماً به State همان Home منتقل می‌شود و بنر نصب و
پیشرفت دانلود همان‌جا، حتی پس از «بعداً» قبلی، دیده می‌شود. مرجع این Hotfix فایل‌های
`DEVELOPMENT_LOG_0.16.4.md`، `TEST_REPORT_0.16.4.md` و
`RELEASE_NOTES_0.16.4.md` است.

## 0.14.5 — Cinema Polish — انجام و منتشر شد

- Race کاتالوگ/DOM در SPA پارسی‌فلیکس بسته شد؛ صفحه اصلی دیگر به نام فیلم
  در اخیراً بازشده ثبت نمی‌شود و رکورد قدیمی از کاتالوگ ترمیم می‌شود.
- Quick Glance بدون اسپویل با Hover موس و Focus ریموت، خلاصه/سال/ژانر/امتیاز/
  مدت/قسمت منتشرشده را از داده عادی خود سرویس نشان می‌دهد.
- همه Railهای Home به LazyRow منتقل شدند؛ فلش‌ها با LazyListState صفحه‌بندی
  می‌کنند و فقط کارت‌های قابل مشاهده Decode/Compose می‌شوند.
- APK کانال Update از حدود 76MB به حدود 22.2MB رسید؛ اینترو و Browser حذف نشدند.
- مرحله بعد `0.14.6 — Pointer & Playback Polish` است: پخش خودکار امن
  FilmRooz و تعامل مشترک Focus/Hover کارت‌ها با Scale و سایه نرم.

## 0.14.6 — Pointer & Playback Polish — انجام و منتشر شد

- پخش خودکار ParsiFlix بدون تغییر سالم ماند.
- FilmRooz پس از رسیدن به صفحه پلیر Play خودکار امن انجام می‌دهد؛ اگر سیاست
  مرورگر مانع شود، کنترل دستی فعلی بدون گیر در دسترس می‌ماند.
- Focus ریموت و Hover واقعی موس اکنون یک زبان مشترک دارند: Scale کوتاه، سایه
  و روشنایی نرم؛ Border قرمز حذف شد.
- مکث موس Quick Glance را نمایش می‌دهد و Click همچنان محتوا را باز می‌کند.
- حرکت‌ها سبک، بدون جابه‌جایی Rail و مناسب Android Box کم‌قدرت پیاده شدند.

## 0.15.0 — Aminema Spotlight — انجام و منتشر شد

- صفحه جزئیات Native برای فیلم و سریال ایرانی و خارجی.
- کلیک اول Spotlight و کلیک دوم Watch/Continue موجود را بدون تغییر باز می‌کند.
- Live TV همچنان مستقیم و یک‌کلیکی است.
- عنوان‌های قدیمی Recent/Continue هنگام بازشدن از صفحه عادی و لاگین‌شده
  سرویس تکمیل می‌شوند: سال، امتیاز، مدت، ژانر، کشور، زبان، خلاصه، کارگردان
  و بازیگران اصلی.
- وضعیت تأییدشده `دوبله فارسی` سبز و `زیرنویس فارسی` آبی است؛ منوی عمومی
  سایت هرگز به‌عنوان وضعیت خود فیلم حساب نمی‌شود.
- هیچ جمله ساختگی برای «اطلاعات موجود نیست» نمایش داده نمی‌شود؛ بخش واقعاً
  خالی فضا اشغال نمی‌کند.
- سال کنار عنوان است و چیدمان 720p/1080p اطلاعات و دکمه‌ها را داخل Safe Area
  نگه می‌دارد.

## 0.15.1 — Cinematic Home — انجام و منتشر شد

- Home از Provider-first به Content-first تبدیل شد: یک Hero هوشمند از
  Continue، سریال‌های من و تازه‌های ایرانی/خارجی.
- Hero هر ۱۱ ثانیه آرام عوض می‌شود، هنگام Focus/Hover متوقف می‌ماند و کلیک
  همان Spotlight و مسیر پخش اثبات‌شده قبلی را باز می‌کند.
- پس‌زمینه پوستر مورد Focus را با Crossfade و Dwell کوتاه دنبال می‌کند تا
  حرکت سریع ریموت Decode اضافی نسازد.
- هدر و Greeting کم‌ارتفاع شدند تا Moment اصلی کامل داخل Viewport تلویزیون
  دیده شود.
- کارت‌های بامزه ایرانی/خارجی حذف نشدند؛ به ورودی مستقیم کم‌حجم در انتهای
  Home منتقل شدند. فقط در Cold start کاملاً خالی نزدیک بالا می‌آیند.
- Focus ریموت و Hover موس همان Scale، Lift و Brightness نرم را دارند؛
  Border قرمز برنگشته است.

## 0.16.0 تا 0.16.2 — Hero، Artwork و Search Polish

- 0.16.0 جداسازی قطعی پوستر عمودی و تصویر عریض، Hero تازه و ردیف‌های بنر
  برگزیده را اضافه کرد.
- 0.16.1 ظاهر Hero/Spotlight و fallback خلاصه را اصلاح کرد، اما مشخصات داخلی
  APK به‌اشتباه روی 0.16.0/code 33 باقی ماند و GitHub Release آن Asset ندارد.
- 0.16.2 شناسه داخلی را به code 34 می‌برد، Decode و Motion اضافه Hero را حذف
  می‌کند، پوستر واقعی منتخب را از بنر پس‌زمینه جدا می‌کند و RTL کامل Hero را
  می‌سازد. خلاصه سریال‌های ایرانی/خارجی با Merge غیرمخرب بازیابی می‌شود و
  Spotlight فقط فیلدهای خالی را با Wikipedia/Wikidata معتبر و نام فارسی عوامل
  تکمیل می‌کند. بنرها خواناتر و Back جستجو قابل‌دیدن و مطمئن شده‌اند.

## صف نسخه‌های قطعی بعدی — اولویت بازطراحی‌شده پس از بررسی منبع سوم

جزئیات Probe و تصمیم معماری MyMoviz در
`MYMOVIZ_PRODUCT_ANALYSIS.md` ثبت شده است.

### 0.16.3 — Episode Navigator ✅ منتشر شد

- انتخاب صریح `Season + Episode`، ماشین حالت Provider-specific و اصلاح پوستر
  در 2026-08-08 روی امولاتور لاگین‌شده تأیید شد. جزئیات Evidence در
  `DEVELOPMENT_LOG_0.16.3.md` و نتیجه تست در `TEST_REPORT_0.16.3.md` است.
- Scope نهایی این نسخه: انتخاب دستی دقیق فصل/قسمت، Resolver پیشنهادی FilmRooz،
  جلوگیری از Race با Direct Play و ترمیم پوستر Recent/Continue.
- موارد پیشرفته زیر (سه Action هوشمند، Preferred Edition و Progress
  Edition-aware) Backlog نسخه‌های بعدی‌اند و جزو ادعای Release 0.16.3 نیستند.

### 0.16.4 — In-app Update Banner Hotfix ✅ منتشر شد

- ریشه باگ: Settings فقط Toast نشان می‌داد و Release پیدا‌شده را به
  `HomeViewModel` نمی‌داد؛ Home در حالت `Idle` یا نسخهٔ Skipشده باقی می‌ماند.
- `UpdateRepository` اکنون State واحد سطح Application را در اختیار Settings و
  Home می‌گذارد؛ بنر نصب/پیشرفت داخل Settings و بدون چک شبکه‌ای دوم دیده
  می‌شود و با خروج از Settings همان State در Home باقی می‌ماند.
- انتخاب «بعداً» از DataStore پاک نمی‌شود؛ فقط درخواست دستی جاری آن را برای
  همان نشست نمایش می‌دهد.
- Package، Signing، Cookie، Login، Library و Cacheهای کاربر تغییر نکرده‌اند.

- نمایش صریح `فصل X • قسمت Y` زیر تمام کارت‌های سریال.
- انتخاب فصل و قسمت در Spotlight پس از Probe کنترل‌های عادی هر Provider.
- FilmRooz نیازمند ماشین حالت صریح
  `انتخاب فصل → انتخاب کیفیت → فعال‌سازی پخش آنلاین → انتخاب قسمت` است؛
  خاکستری‌بودن Episodeها پیش از Action و آبی‌شدن آن‌ها پس از Action باید با
  DOM واقعی تأیید شود.
- DOM probe لاگین‌شده تأیید کرد `select#cseason` و Containerهای
  `#cseason_{n}` تمام فصل‌ها را از ابتدا در صفحه دارند؛ Catalog همه فصل‌ها در
  یک Pass استخراج و Cache شود و فقط عملیات پخش وارد ماشین حالت WebView شود.
- Adapter از `.eSbox/.eDbox`، `data-action=stream|dl` و آیکن‌های معنایی
  `check/play-circle` استفاده کند؛ Download modal هرگز Episode stream تلقی نشود.
- سه Action جدا: `ادامه قسمت بعد`، `آخرین قسمت منتشرشده` و
  `انتخاب فصل و قسمت`.
- Continue فقط از Playback واقعی، Checkmark معتبر یا Baseline دستی.
- ترجیح هر سریال بین `ادامه داستان` و `همیشه آخرین انتشار`.
- Resolver قطعی پخش: دوبله/دو‌زبانه 1080p، سپس زبان اصلی 1080p و بعد
  Fall back به 720p و 480p؛ کیفیت 2160p خودکار انتخاب نشود.
- Editionهای Normal، Uncut، Extended، DVD و BluRay جدا از Quality مدل شوند؛
  ابتدا Edition و بعد زبان/کیفیت انتخاب شود.
- انتخاب Edition هر عنوان ذخیره و Progress هر Episode به‌صورت Edition-aware
  نگهداری شود؛ تیک‌های نسخه BluRay و DVD/Uncut با هم Merge نشوند.
- Quality و Audio به‌عنوان Rendition یک PlaybackIdentity مدل شوند؛ Progress
  میان 1080/720/480 و دوبله/زبان اصلی مشترک و فقط بین Timelineهای متفاوت جداست.
- در تعارض کیفیت و کامل‌بودن، انتخابگر کوتاه شامل نام نسخه و مدت تقریبی فقط
  بار اول نمایش داده شود.
- Edition UI با Progressive disclosure باشد: برای اکثریت عناوین کاملاً مخفی؛
  فقط در Multi-edition گزینه کم‌اهمیت `نسخه‌های دیگر` نشان داده شود.
- انتخاب جایگزین بین `فقط همین قسمت` و `از این به بعد این نسخه` تفکیک شود؛
  تماشای مقایسه‌ای یک Episode نباید Preferred Edition سریال را تغییر دهد.
- Progress دو لایه باشد: Canonical episode completion برای Next Episode و
  Variant-specific position برای Resume دقیق همان Cut.
- Multi-editionها خودکار کشف شوند؛ نیاز به لیست دستی عنوان‌ها نباشد. Parser با
  حذف Source/Resolution/Language/Size/Duration، Edition label باقی‌مانده و
  اختلاف مدت/همسانی Episode set را با Confidence ارزیابی کند.
- Label ناشناخته Merge نشود و در Diagnostics محلی ثبت شود؛ فقط Confidence بالا
  یا متوسط UI `نسخه‌های دیگر` را فعال کند تا Sourceهای معمولی اشتباه Edition
  شناخته نشوند.
- Episode identity از Raw label/range نیز پشتیبانی کند تا قسمت‌های Combined،
  Split، Special، Bonus، Episode 0 و شماره‌گذاری متفاوت اشتباه Merge نشوند.
- `PlaybackSessionController` اضافه شود: Auto-next داخل فصل پذیرفته شود، اما Back
  از Player با یک Action، Fullscreen را ببندد و مستقیماً Native Episode Navigator
  را باز کند؛ زنجیره History قسمت‌ها و صفحه WebView واسطه نمایش داده نشوند.
- انتهای فصل CTA بومی `فصل بعد • قسمت ۱` داشته باشد؛ Auto-play واقعی فقط پس از
  Probe سیاست Player/Autoplay فعال شود.
- هر Auto-next تأییدشده، `currentEpisodeKey` و Continue Watching را به Episode
  جدید منتقل کند؛ Session نباید روی قسمت اولیه ثابت بماند.
- Episode summary و Spoiler Shield در این نسخه ساخته نشوند؛ Provider فقط خلاصه
  کلی سریال دارد و Summary قسمت نیازمند Source مستقل معتبر است.
- Native playback-start settings ساخته نشود؛ Resolver بهترین Seed را انتخاب کند
  و Quality/Audio/Channel داخل Player فعلی مدیریت شوند.
- واژه `دیده‌نشده` فقط با Evidence دقیق؛ در غیر این صورت
  `قسمت جدید از آخرین وضعیت ثبت‌شده`.

### 0.16.4 — Canonical Library, Dedupe & Smart Search

- مدل `CanonicalMedia + SourceVariant`؛ یک عنوان، یک کارت، چند نسخه.
- حذف تکراری ParsiFlix/FilmRooz با پذیرش اولیه Query فارسی `لیسانسه`.
- Match قطعی IMDb، سپس عنوان Normalizeشده + سال + نوع؛ Match مبهم Merge نشود.
- Query normalization برای فاصله، خط تیره، Apostrophe، نیم‌فاصله و `ي/ی`،
  `ك/ک`.
- Variantهای `spiderman`، `spider man` و `spider-man` برای Provider مناسب.
- انتقال Continue/Favorite/Recent از Provider card به CanonicalMedia.
- بخش کم‌حجم `نسخه‌های موجود` در Spotlight بدون شلوغ‌کردن Home.

### 0.16.5 — My Series

- Follow دستی سریال و Baseline `تا فصل/قسمت دیدم` به‌عنوان مسیر سریع.
- Rail/صفحه `سریال‌های من`: `قسمت بعدی`، `۲ قسمت جدید`، `فصل جدید`،
  `به‌روز هستی`.
- Probe دقیق Checkmarkهای FilmRooz و مدل Progress حساب Provider.
- ترکیب Playback محلی، Checkmark معتبر و Baseline دستی با Evidence level.
- بررسی مسیر ایرانی پس از تثبیت سریال خارجی.
- اعلان داخل اپ فقط برای Episode/Season واقعاً جدید؛ بدون Spam.

### 0.16.6 — Cinematic Promo Feed

- Promo/Carousel عادی ParsiFlix و FilmRooz به Hero واحد 0.15.1 اضافه می‌شود.
- چرخش آرام، توقف روی Focus/Hover و بدون دزدیدن Focus.
- Banner عنوان → Spotlight؛ Banner Live TV → اجرای مستقیم یک‌کلیکی.
- حذف Banner تکراری با Canonical ID.
- Action دستی Refresh برای هر Provider و Sync پس‌زمینه بدون قفل‌کردن اپ.

### دروازه تحقیق قبل از 0.17.0 — Third Source Coverage Lab

- نمونه حداقل 100 عنوان برای اندازه‌گیری هم‌پوشانی واقعی.
- گزارش Match قطعی IMDb، Match عنوان/سال، عنوان فقط MyMoviz و مزیت دوبله.
- تست Login/session، سرعت Catalog/Detail و Normal Watch route.
- هیچ درصدی بدون گزارش نمونه اعلام نمی‌شود.

### 0.17.0 — MyMoviz Provider

- Adapter مستقل برای Catalog، Search، Detail metadata و Normal Watch.
- ورود فقط Coverage Gap و نسخه دوبله بهتر؛ نه ساخت Rail تکراری سوم.
- استفاده از شناسه IMDb برای Dedupe دقیق.
- پشتیبانی از آخرین فصل/قسمت، Progress عادی حساب و `قسمت بعدی شما`.
- حفظ کامل مرز امنیتی: فقط صفحه عادی سایت، بدون Media URL/Token/DRM.

### 0.17.1 — Best Source Resolver

- تنظیم `دوبله اول | زبان اصلی اول | هر بار بپرس`.
- انتخاب نسخه با اولویت زبان، سلامت Provider و موجودبودن.
- یک کارت در Home و انتخاب Source فقط در Spotlight/Quick menu.
- ممنوعیت تعویض خودکار Source وسط پخش.
- زیرساخت آماده برای منبع ایرانی چهارم با همان Adapter contract.

### 0.17.2 — People, Filmography & Alerts

- کلیک روی بازیگر/کارگردان و Filmography ادغام‌شده.
- Follow محلی Person.
- اعلان کم‌تعداد هنگام پیدا شدن اثر جدید در Catalog Sync.
- `افزودن فیلم‌های این شخص به Home` به‌صورت Rail قابل‌حذف.

### 0.18.0 — Cinema Library & Personal Home

- View All برای Latest/Continue/Recent/Favorites/My Series.
- Grid و Filter فیلم/سریال، دوبله، ژانر، سال و وضعیت پخش.
- مقصد `برای من`: Continue، قسمت بعدی، سریال‌های من، لیست من و اعلان‌ها.
- `سنجاق به Home` برای Search، Person، Genre و Collection.
- ترتیب و مخفی‌کردن Railها با محدودیت مناسب TV.

### 0.18.1 — Reliability, Keyboard & Provider Health

- وضعیت Login هر سرویس و تفکیک Internet/DNS/Login/Unavailable.
- Cache آفلاین، Health check سبک و حفظ داده آخرین Sync.
- بازطراحی نهایی Keyboard سایت و Search Deck.
- Back بهتر FilmRooz بر اساس KeyCode واقعی Android Box.
- مهاجرت Home عمودی به LazyColumn اگر تعداد Railها از بودجه Performance عبور کرد.

### 0.18.2 — Rich Details & Parental Guide

- افزودن Parental Guide به صفحه توضیحات با دسته‌بندی روشن خشونت، زبان، مواد،
  محتوای جنسی و شدت؛ بدون کپی متن طولانی یا شلوغ‌کردن Hero.
- Progressive disclosure: در Spotlight فقط رده‌بندی خلاصه و در صفحه جزئیات
  دسته‌بندی کامل Parental Guide؛ داده ناقص باعث Placeholder یا ادعای ساختگی نشود.
- منبع اصلی Provider/IMDb/Wikipedia یا Source معتبر Metadata و Cache محلی؛
  داده فارسی موجود مقدم و ترجمه AI در آینده با Label شفاف انجام شود.
- Episode recap/summary به‌عنوان تحقیق جدا: Source انگلیسی معتبر، ترجمه فارسی و
  امکان Recap فقط تا آخرین Episode دیده‌شده؛ خارج از Scope اولیه این نسخه.

### 0.19.0 — Geek Mode

- Collectionهای نسخه‌بندی‌شده: MCU، Star Wars، Lord of the Rings و Harry Potter.
- ترتیب اکران، ترتیب زمانی، `از اول ببین` و `خفن‌ها را ببین`.
- افزودن Collection به Home و مخفی/تیک‌شدن موارد دیده‌شده.
- Resolve خودکار بهترین SourceVariant موجود.
- توسعه بعدی برای لیست بازیگر و کارگردان.

### Backlog باز و قابل‌افزودن بدون بازطراحی

- Provider ایرانی چهارم پس از دریافت URL و Probe.
- کانال‌های Live TV جدید از JSON و Settings همراه با Health check.
- فیدهای Promo جدید؛ فقط Normal page URL و هرگز Media/Stream URL.
- `Download-to-Stream Bridge`: پس از کلیک عادی کاربر روی Download، پیشنهاد
  پخش تدریجی فایل مجاز با Media3، Cookie/Header همان نشست و بدون ذخیره کامل.
  Range/206، Expiry، زیرنویس و Fallback بدون Seek باید ابتدا Probe شوند. هسته
  RemoteFile Player آن با Telegram مشترک خواهد بود؛ DRM یا Authentication
  هیچ‌گاه دور زده نمی‌شود.

## تصمیم طراحی Home

Home نباید با اضافه‌شدن هر Provider یک Rail تازه و تکراری بگیرد. منبع‌ها در
لایه داده ادغام می‌شوند و صفحه فقط زبان محصولی نشان می‌دهد:

1. **تازه‌های ایرانی** — `همه | فیلم | سریال`
2. **تازه‌های خارجی** — `همه | فیلم | سریال`

اگر یک عنوان در چند Provider باشد، فقط یک کارت نمایش داده می‌شود. نسخه دوبله
یا زبان اصلی در Spotlight انتخاب می‌شود، نه با تکرار کارت روی Home. انتخاب
آخر هر Rail ذخیره می‌شود و `مشاهده همه` Grid کامل همان بخش را باز می‌کند.

### ترتیب پیشنهادی Home

1. هدر: لوگو، جستجوی یکپارچه، Sync providerها و تنظیمات
2. Hero پویا: آخرین Continue یا Promo مهم، بدون دزدیدن Focus
3. ادامه تماشا
4. تازه‌های ایرانی با تب فیلم/سریال
5. تازه‌های خارجی Canonical با تب فیلم/سریال
6. سریال‌های من و قسمت بعدی
7. کتابخانه من: علاقه‌مندی‌ها و اخیراً بازشده‌ها
8. پخش زنده
9. میان‌بر ورود مستقیم به Providerها در انتهای صفحه

کارت‌ها پوستر 2:3، عنوان، نوع، سال، دوبله/زیرنویس و برای سریال آخرین
فصل/قسمت را نمایش می‌دهند. Provider فقط وقتی چند نسخه وجود دارد در Spotlight
یا Quick Menu دیده می‌شود. Focus کنترل و Hover موس از یک زبان بصری مشترک
استفاده می‌کنند.

## منابع کاتالوگ

### ParsiFlix

بخش رسمی «جدیدترین‌ها» در داده حساب بررسی شده و برای هر مورد `id`، عنوان،
`MOVIE/SERIES`، پوستر و لینک پایدار جزئیات دارد. بنابراین تقسیم آن به فیلم و
سریال مستقیم و کم‌ریسک است.

### FilmRooz

- فیلم‌ها: صفحه لاگین‌شده
  `https://sean.robert-redford.net/archive/category/featured-films/`
- سریال‌های به‌روز شده:
  `https://sean.robert-redford.net/archive/series/`
- سریال‌های برگزیده:
  `https://sean.robert-redford.net/archive/playlist/show/most-popular-tv-shows/`
- فقط عنوان، پوستر و لینک پایدار `/post/film|series/...` ذخیره می‌شود.
- پوسترهای نیازمند ورود داخل WebView دانلود و در حافظه خصوصی اپ کش می‌شوند.

### MyMoviz — پس از Coverage Lab

- Home: `https://mymoviz.co/_modern/home`
- لینک پایدار عنوان: `/_modern/title/{id}/{slug}`
- صفحه عادی Watch: `/_modern/watch/{id}` و برای سریال فصل/قسمت عادی سایت.
- شناسه IMDb، دوبله/زیرنویس، Person، آخرین فصل/قسمت و Progress عادی حساب
  قابل‌مشاهده‌اند.
- فقط Coverage gap یا SourceVariant بهتر وارد UI می‌شود.

## جستجوی یکپارچه

### نسخه سریع

- یک Search Bar در هدر Home.
- کاربر عبارت را یک بار وارد می‌کند.
- نتایج عادی جستجوی هر سایت در دو تب `ParsiFlix | FilmRooz` باز می‌شود.
- این نسخه سریع‌تر و مقاوم‌تر است، چون سایت‌ها کنترل نتایج خود را حفظ می‌کنند.

### نسخه کامل پیشنهادی

- جستجوی هم‌زمان هر دو سرویس با تأخیر 400 میلی‌ثانیه پس از تایپ.
- صفحه نتیجه با فیلترهای:
  `همه | فیلم | سریال | دوبله | درحال پخش`
- نتایج Native Canonical با پوستر، عنوان و لینک Spotlight؛ Provider filter
  فقط در بخش پیشرفته یا Diagnostics لازم است.
- در صورت خراب‌شدن Adapter یک سایت، دکمه «جستجو داخل سایت» همیشه به‌عنوان
  راه جایگزین باقی می‌ماند.
- اطلاعات ورود و توکن‌ها داخل WebView باقی می‌مانند و هیچ URL مدیا/استریم
  خوانده یا ذخیره نمی‌شود.

## معماری پیشنهادی

- `CanonicalMedia`: هویت یکتای فیلم/سریال، IMDb، عنوان‌ها، سال، نوع و Metadata
- `SourceVariant`: Provider، صفحه جزئیات، صفحه Watch عادی، زبان و Availability
- `CatalogSection`: مقصد محصولی، فیلتر فیلم/سریال، زمان Sync و Canonical itemها
- `CatalogProvider`: Adapter مستقل برای هر Provider
- `CatalogRepository`: Match، ادغام، حذف تکراری، کش و وضعیت خطا
- `BestSourceResolver`: ترجیح دوبله/زبان اصلی و سلامت Provider
- کش سبک JSON و پوستر خصوصی برای استارت سریع روی Android Box
- Sync خودکار محدود و مستقل در اولین اجرا + Refresh دستی هر بخش؛ نه Poll دائمی

## برنامه نسخه‌ها

### 0.12.1 — Playback Reliability — منتشر شد

- فلش چپ/راست مشترک برای همه ردیف‌های Home و Search
- حذف زیرعنوان اضافی Live
- Continue و Direct Play فقط پس از تأیید Player route موفق محسوب می‌شوند
- Retry امن برای SPA و جلوگیری از Double click
- Loading سینمایی با مسکات و متن بامزه؛ fallback پس از ۱۴ ثانیه
- Sync خودکار Continue حساب در Cold start و Reconcile یکسان بین دستگاه‌ها
- محدودیت پذیرفته‌شده: FilmRooz Series در Account Recent همیشه Exact episode
  نمی‌دهد؛ در این حالت Detail برای انتخاب دستی باز می‌شود.

### 0.13.0 — بازطراحی کامل کیبورد (قدم بعدی پیشنهادی)

هدف: Keyboard یک State Machine واحد و قابل‌پیش‌بینی شود، نه مجموعه‌ای از
Focus patchها:

`Idle → Username → Password → Caps/Language/Show → Submit → Closed`

- Header با عرض ثابت؛ Preview رمز هیچ‌وقت Layout را جابه‌جا نکند.
- Caps، Language و Show/Hide فقط State را عوض کنند، نه View tree/focus target.
- انتخاب صریح بین Aminema Keyboard و System IME در Settings.
- دکمه مستقل **بستن کیبورد** که با موس و DPAD همیشه قابل‌دسترسی باشد.
- تست واقعی Android Box: USB mouse، DPAD، Back، Next، Done و Form rebuild.
- میکروپولیش پیشنهادی: key press نرم، صدای اختیاری بسیار کوتاه و Hint واضح
  «Next: Password» بدون شلوغی.

**وضعیت Track Login Input Deck:** پیاده‌سازی و پذیرش امولاتور انجام شد.
State Machine، DOM token و session id جای Focus patchهای پراکنده را گرفتند.
مسیر Username → Password با Form replacement، Caps، زبان، Show، Submit و
Back تست شد. فقط Acceptance روی Android Box واقعی باقی مانده است.

**Track جداگانه Search Deck خود اپ — پیاده‌سازی اولیه انجام شد:**

- پنل یکپارچه Query + Keyboard با ظاهر سینمایی و CTA قرمز واضح
- QWERTY واقعی و پلکانی فارسی/انگلیسی؛ نه Wrap الفبایی ده‌ستونی
- کلید مستقل `ژ` و `آ` برای جستجوی تلویزیونی بدون Shift/Long press
- فوکوس/هاور روشن‌تر، اندازه‌های ثابت و نمایش کامل بدون Clipping در 1080p
- جمع‌شدن به Query bar بعد از Submit و مخفی‌بودن Resultهای خالی قبل از Search
- وضعیت و شواهد دقیق در `DEVELOPMENT_LOG_0.13.0.md`

### پیش‌نیاز رشد بعدی Home

در 0.14.5 همه ردیف‌های افقی Home به `LazyRow` منتقل شدند تا کارت‌های خارج
Viewport هم‌زمان Compose و Decode نشوند. مهاجرت عمودی از
`Column + verticalScroll` به `LazyColumn` زمانی انجام می‌شود که تعداد
بخش‌های Home باز هم رشد محسوسی کند.

### مرحله صفر — تأیید 0.7.2

- Next از Username به Password
- Done
- Cancel
- Back کنترل
- خروج و کلیک دوباره روی Password با موس
- روشن/خاموش کردن Caps داخل Password بدون برگشت تایپ به Username
- تغییر زبان داخل Password بدون برگشت تایپ به Username

## اصلاحات ثبت‌شده پس از تست روی تلویزیون واقعی

این موارد تأیید شده‌اند و برای یک Maintenance Update آینده نگه داشته می‌شوند؛
فعلاً در 0.7.2 تغییری ایجاد نمی‌کنند.

### بازطراحی کامل کیبورد

- بررسی دوباره جلوگیری از ظاهرشدن ناگهانی کیبورد خود Android Box وسط تایپ.
- تبدیل مدیریت کیبورد به یک State Machine ثابت برای
  `Username → Password → Caps → Language → Show/Hide → Done`.
- طراحی مجدد هدر Password با عرض ثابت و بدون به‌هم‌ریختگی Layout.
- نمایش درست و خوانای رمز تایپ‌شده هنگام Show؛ متن بلند باید داخل ناحیه ثابت
  اسکرول یا Ellipsis شود و باعث کوتاه‌شدن/فشرده‌شدن Preview نشود.
- Caps، زبان و Show/Hide نباید ردیف‌های اصلی کیبورد یا مقصد تایپ را بازسازی
  کنند.
- افزودن تنظیم صریح برای انتخاب «کیبورد Aminema» و در صورت نیاز غیرفعال
  ماندن کامل کیبورد سیستمی.
- تست مستقل روی Android 9، Android TV جدید، موس USB و کنترل DPAD.

### بازبینی Back و دکمه‌های موس در مرورگر FilmRooz

- بازسازی گزارش تلویزیون واقعی که هنگام کار با موس، صفحه به History قبلی
  مرورگر برمی‌گردد.
- اطمینان از اینکه Left Click فقط Click عادی سایت است و هیچ‌وقت نقش Back
  ندارد.
- نگاشت شفاف:
  `Left Click = سایت`، `Right Click = Quick Menu`،
  `Mouse Back/Forward = تاریخچه مرورگر`.
- بررسی یک میان‌بر سریع و مستقل برای «Amin Home» تا بازگشت به صفحه اصلی اپ
  وابسته به History سایت نباشد.
- بررسی رفتارهای پیشنهادی:
  `Back کوتاه = تاریخچه سایت` و `Back طولانی = خانه Aminema`.
- این بخش ابتدا باید با Event Log روی Android Box واقعی عیب‌یابی شود، چون
  مدل‌های مختلف موس/ریموت KeyCode و MotionEvent متفاوتی ارسال می‌کنند.

### 0.8 — بازطراحی Home و زیرساخت کاتالوگ — انجام شد در 0.8.0

پیاده‌سازی و تست‌شده در نسخه 0.8.0:

- مدل `CatalogItem/CatalogSection` و `CatalogRepository` با کش محلی
- دو ردیف تازه‌ها با انتخاب‌گر `همه | فیلم | سریال` و ذخیره انتخاب
- Refresh دستی، زمان آخرین Sync، وضعیت خالی و وضعیت خطا
- Adapter مستقل برای هر سرویس؛ خطای یکی ردیف دیگر را خراب نمی‌کند
- مسیر واقعی «سریال خارجی» از منوی حساب لاگین‌شده تأیید شد:
  `/archive/category/new-tv-show/` (این انتخاب تاریخی در 0.14.0 با فید دقیق‌تر
  `/archive/series/` جایگزین شد)

باقی‌مانده برای آینده:

- هدر فشرده جدید و جای جستجو
- صفحه «مشاهده همه» با Grid کامل
- بروزرسانی خودکار محدود (در 0.14.0 انجام شد)

### 0.9 — جستجوی یکپارچه

- Search Overlay مناسب TV
- جستجوی هم‌زمان دو سرویس
- نتایج گروه‌بندی‌شده و فیلتر فیلم/سریال
- تاریخچه کوتاه عبارت‌های جستجو
- راه جایگزین «جستجو داخل سایت»

### 1.0 — کتابخانه شخصی

- صفحه کامل Favorites، Continue و Recently Opened
- فیلتر و مرتب‌سازی
- حذف/ویرایش آیتم‌ها با Long Press
- Export/Import تنظیمات، سرویس‌ها و کتابخانه محلی

### 1.1 — امکانات هوشمند

- نشان «جدید از آخرین بازدید»
- تشخیص اپیزودهای جدید در داده عادی حساب
- حذف نتایج تکراری عنوان/سال بین سرویس‌ها
- انتخاب سرویس ترجیحی برای یک عنوان
- پیشنهادهای محلی بر اساس ژانرهای بازشده، بدون استخراج استریم

### 1.2 — تجربه Premium

- جستجوی صوتی Android TV
- پروفایل ظاهر و اندازه متن
- انیمیشن‌های سبک‌تر برای Boxهای ضعیف
- Skeleton loading، Empty State و خطاهای اختصاصی هر سرویس
- میان‌برهای قابل تنظیم کنترل و موس

### اینتروی فانتزی و حماسی برند — انجام شد در 0.7.4

این مرحله در نسخه 0.7.4 با ویدئوی نهایی CapCut پیاده‌سازی و تست شد:
پخش فقط در Cold Start، بدون Loop، بدون شبکه، Skip فوری با `OK`/`Back`/کلیک
موس، بازنشدن دوباره هنگام برگشت از مرورگر، Fallback کامل در خطای Decoder و دو
گزینه `Play intro` و `Mute intro` در Settings.

خواسته‌های اولیه این مرحله:

- ساخت یک ویدئوی کوتاه ۳ تا ۵ ثانیه‌ای با حضور کاراکتر اختصاصی برنامه.
- نمایش بزرگ متن:
  **«تقدیم به فارسی‌زبانان کهکشان…»**
- لحن عمداً حماسی، فضایی و بامزه؛ برای مثال ورود کاراکتر از میان ستاره‌ها،
  سیاره‌های پاپ‌کورنی و یک پایان سینمایی اغراق‌آمیز.
- پخش فقط هنگام Cold Start، نه هنگام هر بار برگشت از مرورگر به Home.
- امکان ردکردن فوری با `OK`، `Back` یا کلیک موس.
- اینترو نباید شروع واقعی برنامه را کند کند؛ Home هم‌زمان در پس‌زمینه بارگذاری
  می‌شود و در دستگاه ضعیف نسخه ثابت یا انیمیشن سبک نمایش داده خواهد شد.
- صدا و موسیقی باید اختصاصی یا بدون محدودیت کپی‌رایت باشد و گزینه
  `Mute intro` در تنظیمات اضافه شود.
- نام نهایی برند و کاراکتر پیش از تولید ویدئو قطعی می‌شود.

### لیست ثبت‌شده برای بروزرسانی‌های بعدی

به ترتیب اولویتی که مالک پروژه اعلام کرده:

1. **پخش زنده — انجام شد در 0.12.0.** ردیف بومی ۲۰ شبکه با لوگوی واقعی،
   پخش مستقیم تمام‌صفحه و بازگشت به همان کارت.
2. **Playback Reliability و فلش ردیف‌ها — انجام و منتشر شد در 0.12.1.**
3. **بازطراحی کیبورد — انجام و منتشر شد در 0.13.0.**
4. **Series Pulse — منتشرشده در 0.14.0:** ردیف «سریال‌های من»،
   فید سریال‌های دارای قسمت تازه، برچسب قسمت/فصل و ردیف برگزیدهٔ جدا.
5. **تازه‌های یوتوب** — تب جدید سایت فارسی؛ می‌تواند یک ردیف دیگر شود.
   ضروری نیست، فقط کامل‌کننده است.
6. **بروزرسانی خودکار و بی‌صدا — انجام و منتشر شد در 0.14.0:** هر Provider
   جدا و پشت Home، با حفظ Cache و Spinner کوچک همان ردیف. همگام‌سازی حساب
   شروع برنامه هم دیگر Activity تمام‌صفحه باز نمی‌کند.
7. **Cinematic Hover Preview — انجام شد در 0.14.5:** خلاصه بدون اسپویل، سال، ژانر،
   امتیاز، مدت و آخرین قسمت با تأخیر 600ms و کش سبک.
8. **پیشرفت صادقانه سریال:** `SeriesProgress` محلی و گزینه
   «تا این قسمت دیدم»؛ `دیده‌نشده` فقط با شاهد دقیق.
9. **جستجوی پیشرفته** سایت خارجی (فعلاً جستجوی ساده کافی است)

## باگ‌های باز

### صفحه‌های فهرست در «اخیراً بازشده» ظاهر می‌شوند

گزارش: وقتی در سایت صفحه دسته‌بندی (مثلاً فهرست فیلم‌های ایرانی) باز شود،
همان صفحه در ردیف **Recently Opened** هم می‌آید. آن ردیف باید فقط صفحه
جزئیات یک عنوان را نشان دهد، نه صفحه فهرست.

- فیلتر این ردیف در `HomeViewModel.recentlyOpened` است و از
  `contentUrlPatterns` و `excludedUrlPatterns` هر سرویس استفاده می‌کند.
- روی کاغذ این حالت باید همین حالا هم فیلتر شود، پس یک آدرس مشخص دارد از
  فیلتر رد می‌شود. **اول باید دقیقاً بازتولید شود** (کدام سرویس، کدام صفحه،
  چه URLی ذخیره شده) و بعد الگو اصلاح شود — نه اینکه الگوها را کورکورانه
  سفت‌تر کنیم و صفحه‌های درست هم حذف شوند.

### ادامه تماشا از دستگاه‌های دیگر — بخش زیادی‌اش موجود است (به‌روزشده در 0.11.0)

**در 0.11.0 اصلاح شد:** سقف مصنوعی ۱۲ آیتمی که خود کد روی فهرست هر سایت
می‌گذاشت (در حالی که لایه ادغام تا ۳۰ تا را می‌پذیرفت) به ۳۰ رسید؛ برای
فیلم خارجی هم پیدا کردن بخش «مشاهدات اخیر» به‌جای یک تلاش با تأخیر ثابت،
چند بار با فاصله امتحان می‌شود (سایت ممکن است کندتر لود شود). این دو، ریشهٔ
گزارش «گاهی از دستگاه دیگر نمی‌آید» بودند.

**هنوز مانده — همان قبلی:**

این قابلیت با دکمه **Sync accounts** از نسخه 0.7 وجود دارد و در تست همان
نسخه با داده واقعی کار کرده: بخش «ادامه تماشا»ی حساب سرویس ایرانی و
«مشاهدات اخیر» پنل کاربری سرویس خارجی خوانده و با Continue محلی ادغام
می‌شوند (در آن تست ۸ و ۱۱ ردیف، مجموعاً ۱۹ نشست ادغام‌شده).

آنچه واقعاً مانده:

1. **دستی بودن.** باید خودکار شود، هم‌راستا با بروزرسانی بی‌صدای اولین اجرا.
2. **فهرست سرویس خارجی ترکیبی است** از «کامل دیده‌شده» و «نیمه‌کاره». الان
   هر دو وارد Continue می‌شوند، پس فیلمی که تمامش کرده‌ای هم آن‌جا می‌ماند.
   باید بررسی شود که آیا سایت درصد پیشرفت یا نشانه‌ای برای تمام‌شده دارد که
   بشود کامل‌دیده‌ها را کنار گذاشت.
3. **دقت مورد به مورد.** برای سرویس خارجی، داده حساب صفحه محتوا را می‌دهد
   ولی قسمت/کیفیت/زمان دقیق را نه؛ این محدودیت از قبل ثبت شده است.

### وضعیت قسمت/فصل زیر نام سریال

**وضعیت: در 0.14.0 برای FilmRooz انجام و منتشر شد.** `CatalogItem` اکنون
`episodeLabel` دارد و `CatalogCard` یک خط ثابت زیر عنوان رزرو می‌کند. نمونه
واقعی پذیرفته‌شده: Silo → `قسمت ۰۴ فصل سوم`.

روی کارت‌های سریال، زیر عنوان و کوچک‌تر، وضعیت قسمت نمایش داده شود تا معلوم
باشد چه چیزی تازه آمده. **حداکثر یک خط**، با Ellipsis، بدون به‌هم‌ریختن
طراحی فعلی کارت.

- **سرویس خارجی: داده‌اش موجود و تأییدشده است.** در همان کارت‌هایی که الان
  می‌خوانیم متن‌هایی مثل `قسمت ۱۶ فصل اول در حال پخش`، `قسمت ۱۰ فصل اول
  پایان فصل` و `قسمت ۰۱ در حال پخش` وجود دارد. جالب اینکه استخراج عنوان ما
  همین رشته را عمداً رد می‌کند تا با عنوان اشتباه گرفته نشود؛ کافی است
  به‌جای دورانداختن، در یک فیلد جدا نگه داشته شود. کار کمی است.
- **فقط بخش قسمت و فصل نگه داشته شود:** `قسمت ۱۶ فصل اول` یا `قسمت ۰۱`.
  دنباله‌های وضعیت مثل `در حال پخش` و `پایان فصل` حذف شوند، چون متن را الکی
  طولانی می‌کنند و همان چیزی که لازم است را اضافه نمی‌کنند.
- **سرویس ایرانی: بررسی شد و در فهرست موجود نیست.** آیتم‌های فهرست فقط
  `title, type, id, genres, published, publishedAt` دارند و `metaData` در
  نمونه واقعی فصل/قسمت نداشتند. برای حفظ مرز امنیتی و Performance، 0.14.0
  برای هر کارت درخواست Detail اضافی نمی‌زند و عددی حدس نمی‌زند.

کش قدیمی `catalog.json` به‌خاطر مقدار پیش‌فرض فیلد تازه و
`ignoreUnknownKeys` بدون Migration مخرب باز می‌شود.

### نتیجه قطعی تیک‌های قسمت FilmRooz

با همان حساب روی لپ‌تاپ و امولاتور تست شد. تیک‌های Silo و The Handmaid’s
Tale بین دستگاه‌ها کامل همگام نبودند؛ بنابراین این تیک‌ها Signal محلی مرورگر
هستند، نه تاریخچه قطعی حساب. سیاست محصول:

- `قسمت ۰۴ فصل سوم` یعنی آخرین قسمت **منتشرشده**
- `قسمت تازه` بعداً می‌تواند از Delta کاتالوگ نسبت به Baseline محلی بیاید
- عبارت `دیده‌نشده` فقط وقتی نمایش داده می‌شود که Aminema شاهد دقیق داشته
  باشد یا کاربر «تا این قسمت دیدم» را تأیید کند

### پخش زنده + تازه‌های یوتوب — با هم

**یوتیوب فارسی: انجام شد در 0.11.0 و همان‌طور می‌ماند.** ردیف «بیشتر»
پایین‌تر در Home، باز کردن `/youtube-persian` در `BrowserActivity` —
صفحه‌ای عادی، نیازی به تغییر ندارد.

**پخش زنده در 0.11.0 اشتباه پیاده شد؛ در 0.12.0 کامل بازطراحی شد.** نسخهٔ 0.11.0 فقط
دکمه‌ای در هدر بود که صفحهٔ خودِ ParsiFlix (`/medias/live/`) را باز می‌کرد؛
کاربر این را نخواسته بود. **طرح درست، تأییدشده در گفتگو:**

- یک ردیف بومی **پایین صفحه اصلی** (نه هدر)، دقیقاً هم‌شکل با
  `CatalogSectionRow` — کارت هر کانال با لوگوی واقعی‌اش (لوگوها از همین حالا
  به‌عنوان URL مستقیم CDN شناخته‌شده‌اند، از بازرسی `/medias/live/`).
- کلیک روی کارت → مستقیم فول‌اسکرین همان کانال، بدون باز شدن صفحهٔ فهرست یا
  نیاز به لمس اضافه (تفاوت اصلی با 0.11.0).
- به همین ترتیب قابل‌گسترش می‌ماند: وقتی لینک یک کانال دیگر (حتی از سایتی
  غیر از ParsiFlix) پیدا شد، فقط یک آیتم به همین فهرست اضافه می‌شود.

**نتیجهٔ 0.12.0:**

1. هر ۲۰ مسیر با کلیک واقعی روی حساب لاگین‌شده کشف شد؛ شناسه‌ها:
   `51–59, 22, 37, 29, 26, 30, 33, 34, 69, 72, 67, 62`.
2. راه‌حل CSS آزموده و تأیید شد: خود `<video>` با
   `position:fixed; width:100vw; height:100vh` صفحه را کامل می‌گیرد و پخش
   بدون User Gesture دوم شروع می‌شود.
3. مدل `liveTv.channels` داخل `services.json` ساخته شد؛ هر آیتم
   `id/name/path/logoUrl` دارد و `ServicesRepository` آن را به نصب‌های قبلی
   مهاجرت می‌دهد.
4. ردیف بومی با D-pad، موس، فوکوس قرمز، اسکرول افقی و Back به همان کارت
   روی امولاتور واقعی تأیید شد.

### سبک‌سازی و سرعت — پیش‌نیاز ردیف‌های بیشتر

هدف: با اضافه‌شدن فیچرها، برنامه سنگین‌تر و کندتر نشود.

- **مهم‌ترین کار، و باید قبل از افزودن ردیف‌های ژانری انجام شود:** صفحه اصلی
  الان `Column` + `verticalScroll` است، یعنی همه ردیف‌ها یک‌جا ساخته می‌شوند.
  با ۸ تا ۱۰ ردیف روی Android Box ضعیف این محسوس می‌شود. باید به
  `LazyColumn` با `LazyRow` داخلش تبدیل شود تا فقط چیزی که دیده می‌شود ساخته
  شود.
- APK دیباگ فعلی حدود ۷۵ مگابایت است؛ بخش زیادی‌اش ابزار دیباگ Compose است.
  یک Build ریلیز با R8/شرینک، حجم و زمان راه‌اندازی را واقعاً کم می‌کند.
- درخواست پوستر با اندازه مشخص و سیاست کش، به‌جای اندازه پیش‌فرض.
- بروزرسانی کاتالوگ در پس‌زمینه (مورد ۴ بالا) حس سرعت را هم بهتر می‌کند.
- اندازه‌گیری قبل و بعد، نه حدس: زمان تا اولین فریم Home و مصرف حافظه.

### ردیف‌های ژانری در ادامه اسکرول

بالای صفحه شلوغ نشود؛ این‌ها پایین‌تر بیایند:

- انیمیشن‌های جدید، ترسناک‌های جدید، معمایی، مستند
- زیرشاخه‌های جدید
- دوبله فارسی‌های جدید

منابع واقعی که در بازرسی دیده شده و می‌شود استفاده کرد:
`/archive/film/genre/<genre>/`، `/archive/category/dubbed-films/`،
`/archive/category/new-films/`، و در سرویس ایرانی بخش‌های ژانری خود صفحه
اصلی حساب به‌همراه `genres[]` هر آیتم.

### «پیشنهاد بده» — دستیار حال‌وهوا

ایده: یک دکمه که چند سؤال کوتاه می‌پرسد و بعد فیلم پیشنهاد می‌دهد.

سؤال‌های پیشنهادی (هرکدام تک‌کلیک، حداکثر سه تا):

1. الان چه حالی داری؟ `بخندیم | بترسیم | اکشن | آرام و درام | خانوادگی`
2. جدید یا کلاسیک؟ `تازه | قدیمیِ خوب | فرقی ندارد`
3. تنها یا مهمان داری؟ `تنها | دونفره | جمع خانوادگی`

چرا شدنی است: در سرویس خارجی هر کارت **ژانر، کشور، سال، مدت، امتیاز IMDb و
امتیاز منتقدین** را نشان می‌دهد و ژانرها مسیر آرشیو اختصاصی دارند؛ در سرویس
ایرانی هر آیتم `genres[]` و `publishedAt` دارد. پس پاسخ‌ها به یک «نقشه
پرس‌وجو» تبدیل می‌شوند: ژانر ← مسیر آرشیو، تازه/کلاسیک ← بازه سال یا آرشیو
جدیدها، جمع ← فیلتر خانوادگی و حذف رده‌های نامناسب.

قواعدی که باید رعایت شود:

- نتیجه از همان صفحه‌های عادی سایت می‌آید؛ هیچ لینک مدیا خوانده نمی‌شود.
- تعداد درخواست شبکه کم بماند (از کش ژانرها استفاده شود)، وگرنه با هدف
  سبک‌سازی تناقض پیدا می‌کند.
- دکمه «یکی دیگه» برای پیشنهاد بعدی، بدون پرسیدن دوباره سؤال‌ها.
- اگر چیزی پیدا نشد، به‌جای صفحه خالی، شرط‌ها یکی‌یکی شل شوند.

رفتارهایی که عمداً همین‌طور می‌مانند:

- بعد از شروع پخش، `Back` به صفحه خود فیلم برمی‌گردد نه به Home. این عمدی
  است: کاربر می‌تواند دستی کیفیت را عوض کند یا قسمت دیگری از سریال را
  انتخاب کند.

## Hero — بازخورد طراحی روی نسخه واقعی (ثبت‌شده، هنوز اصلاح نشده)

دکمه‌ها دیگر بریده نمی‌شوند (رفع‌شده). ایرادهایی که مالک پروژه روی پنج اسلاید
واقعی دید و باید حل شوند:

**۱. هر اسلاید شکل دیگری دارد — ساختار ثابت نیست.**
چون هر بخش «اگر داده داشت نمایش بده» است، اسکلت اسلاید با داده عوض می‌شود و
دکمه‌ها هر بار در ارتفاع دیگری می‌نشینند؛ با چرخش کاروسل، صفحه بی‌قرار به نظر
می‌رسد. نمونه‌های واقعی:

| اسلاید | خلاصه | قسمت/فصل | سال | امتیاز |
|---|---|---|---|---|
| A Toxic Love Story | دو خط | — | چیپ | ★۶.۷ |
| House of the Dragon | یک خط | دارد | **هم در عنوان هم چیپ** | دارد |
| Supergirl | دو خط | — | **هم در عنوان هم چیپ** | ★۶ |
| کلاغ | دو خط | — | ندارد | ندارد |
| The Hawk | **ندارد** | دارد | چیپ | ندارد |

راه‌حل: اسکلت ثابت با جای رزرو‌شده — عنوان در جعبه دوخطی ثابت، یک نوار
متادیتا با ارتفاع ثابت، خلاصه در جعبه دوخطی ثابت (حتی وقتی خالی است)، تا
دکمه‌ها همیشه در یک ارتفاع بمانند.

**۲. سال دو بار نمایش داده می‌شود.** خود سایت در عنوان `(2022)` می‌گذارد و ما
هم چیپ سال داریم. `SpotlightScreen` این را با شرط
`!item.title.contains(item.year)` حل کرده؛ Hero همان منطق را ندارد.

**۳. تراکم متادیتا بین دو سرویس یکسان نیست.** آیتم‌های ایرانی اغلب سال، امتیاز
و قسمت ندارند («کلاغ» فقط چیپ «سریال» داشت)، پس نوار متادیتا تقریباً خالی
می‌ماند. یا باید حداقل ثابتی تعریف شود یا چیدمان جوری باشد که کم‌بودن داده
تُوخالی به نظر نرسد.

**۴. پوستر بدون هیچ پرداخت گرافیکی است — «خشک و خالی» و به چشم نمی‌آید.**
الان فقط یک کارت با گوشه گرد و یک خط موی سفید است. لازم دارد: سایه//Elevation
واقعی، درخشش ملایم لبه، و یک پایه یا Gradient که کارت را از پس‌زمینه جدا کند.

**۵. کیفیت خود پوسترها یکسان نیست (مشکل داده، نه طراحی).** پوستر
«A Toxic Love Story» عملاً یک فریم از ویدیو **همراه با نوار کنترل پلیر** است، و
پوستر «کلاغ» متن عوامل را بریده نشان می‌دهد. این دقیقاً همان دلیلی است که
بنرهای افقی خود سایت (بخش بالاتر) گزینه بهتری هستند — برای Hero هم باید
بررسی شود، نه فقط برای ردیف «برگزیده‌ها».

## Hero — متن تیره روی پوسترهای روشن (موکول‌شده به‌خواست مالک پروژه)

رنگ Hero از خود پوستر گرفته می‌شود و برای پوسترهای تیره خوب کار می‌کند. برای
پوسترهای **روشن** — «کلاغ» که کِرِم است، یا هر پوستر سفیدِ دیگر — رنگ استخراجی
روشن است و متن سفید روی آن کم‌کنتراست می‌شود.

راه‌حل درست: رنگ متن هم باید با پوستر عوض شود، نه فقط پس‌زمینه. یعنی روشنایی
رنگ استخراج‌شده اندازه‌گیری شود و اگر از یک آستانه بالاتر بود، متن و چیپ‌ها به
تیره سوییچ کنند (همان کاری که Material با `onSurface` می‌کند). چون سقف روشنایی
`deepenForScrim` فعلاً جلوی خیلی روشن‌شدن را می‌گیرد، مشکل حاد نیست ولی حل هم
نشده است.

مالک پروژه این را دید و گفت برای بروزرسانی‌های بعدی بماند.

## بنرهای عریض خود سایت‌ها — Probe انجام شد، داده واقعی (۲۰۲۶-۰۷-۲۹)

هر دو سایت علاوه بر پوستر عمودی، **هنر عریض (Key art)** هم دارند. همه ابعاد
زیر با بارگذاری واقعی تصویر روی حساب لاگین‌شده **اندازه‌گیری** شده، حدس نیست.

### سرویس ایرانی — هر آیتم از قبل دو تصویر دارد

| فیلد | نمونه‌های اندازه‌گیری‌شده | شکل |
|---|---|---|
| `cover` / `coverLink` | ۱۲۰۰×۶۸۰ ، ۱۲۸۰×۷۴۸ ، ۱۷۷۹×۱۰۰۰ ، ۲۵۵۹×۱۱۹۳ | **عریض** ۱.۷۱ تا ۲.۱۵ به ۱ |
| `thumbnail` / `thumbnailLink` | ۱۳۵۰×۱۶۸۷ ، ۱۰۸۰×۱۹۲۰ | **عمودی** ۰.۸۰ و ۰.۵۶ به ۱ |

- کاروسل بنر = `sections[0]` با `type: "CUSTOM"`، آیتم‌ها `itemType: "SLIDER"`.
  فیلدها: `link`, `cover`, `title`, `itemType`, `thumbnail`, `description`.
- **`thumbnail` آیتم‌های SLIDER خالی است** — فقط تصویر عریض دارند.
- `link` آدرس کامل صفحه محتواست (`…/medias/series/345`)، پس کلیک بدون کار اضافه
  درست کار می‌کند.
- تصاویر روی CDN خودشان است (`cdn.farsifilix-images.legamart.com`)، نه هم‌هاست.
  طبق قانون Cookie برایشان Cookie ارسال نمی‌شود — و لازم هم نیست، چون در همین
  Probe بدون Cookie بار شدند.
- **بدون هیچ درخواست شبکه اضافه:** این داده در همان پاسخ `/app/home` که الان
  می‌گیریم هست.

### سرویس خارجی — کاروسل Bootstrap در صفحه اصلی

- ساختار: `div.carousel.slide` ➜ `div.carousel-inner` ➜ `a.carousel-item` ➜ `img`
- تصاویر `/img/1504-846/<hash>.jpg.webp` — اندازه‌گیری‌شده **۱۵۰۴×۸۴۶، دقیقاً
  ۱۶:۹**. شش آیتم در کاروسل.
- `href` هر آیتم مستقیم صفحه محتواست: `/post/film/5738383/the-sheep-detectives/`
- **اندازه داخل مسیر تصویر کدگذاری شده** (`/img/1504-846/`). اندازه دیگری هم روی
  همان صفحه دیده شد: `/img/746-134/` (نسبت ۵.۵۷:۱، بنر باریک سریال).
  **فقط از اندازه‌هایی استفاده شود که خود سایت استفاده می‌کند**، نه دلخواه.
- هم‌هاست است، پس ارسال Cookie مجاز است.
- هزینه: کاروسل روی **صفحه اصلی** است ولی `CatalogBackgroundSync` فعلاً
  `new-films`/`new-tv-show` را می‌بیند ➜ یک بازدید صفحه اضافه لازم است، فقط یکی،
  نه به‌ازای هر عنوان.

### باگ واقعی که همین Probe لو داد — پوستر عمودی با تصویر عریض پر می‌شود

در **چهار جای کد** این الگو هست:

```
posterUrl: item.coverLink || item.thumbnailLink || ''
```

`browser/AccountSyncActivity.kt:239` ·
`browser/CatalogBackgroundSync.kt:437` ·
`browser/CatalogBackgroundSync.kt:500` ·
`browser/SiteSearchEngine.kt:179`

یعنی **همیشه تصویر عریض اولویت دارد**، بعد داخل کارت عمودی ۲:۳ با
`ContentScale.Crop` بریده می‌شود. این ریشه دقیق بی‌ریختی «کلاغ» است که دیده شد
(متن عوامل نصفه، دو طرف بریده) و ایراد شماره ۵ بخش «بازخورد طراحی Hero».

**اصلاح:** برای پوستر عمودی اولویت برعکس شود (`thumbnailLink` اول)، و
`coverLink` در فیلد تازه `backdropUrl` نگه داشته شود تا Hero و ردیف بنری از آن
استفاده کنند. یک Probe، دو مشکل را هم‌زمان حل می‌کند: پوسترهای بی‌ریخت، و
نداشتن هنر عریض.

### تقسیم کار تأییدشده با مالک پروژه

| بخش | منبع تصویر | محتوا |
|---|---|---|
| Hero بالای Home | `backdropUrl` عریض؛ پوستر عمودی فقط Fallback | **جدیدترین‌ها** |
| ردیف «برگزیده‌ها» پایین‌تر | بنر عریض خود سایت | انتخاب سرمقاله‌ای خود سایت |

بنرهای ایرانی **عنوان را داخل تصویر** دارند، پس در ردیف کارت افقی مزیت است و
نیازی به نوشتن عنوان روی آن نیست؛ در Hero باید حواسمان باشد با عنوان خودمان
تداخل نکند. ردیف «سریال‌های برگزیده» فعلی از `most-popular-tv-shows` می‌آید و
**چیز دیگری است** — ردیف بنری سومین مجموعه است، اسمشان باید فرق کند.

## قرارداد شماره‌گذاری نسخه

بعد از `0.9`، بروزرسانی‌های مهم روی `0.10` می‌روند و همین‌طور ادامه پیدا
می‌کنند؛ اصلاح‌ها هم به همین شکل. فعلاً به نسخه `1.0` نمی‌رویم.

### بعدتر

- Watchlist همگام‌شده در صورت پشتیبانی عادی سایت
- اعلان محلی برای محتوای جدید
- Backup رمزگذاری‌شده تنظیمات
- افزودن منبع دوم Live TV در صورت پیدا شدن یک پورتال وب معتبر و قابل‌تست

## خط قرمزهای امنیتی

- بدون دورزدن ورود
- بدون استخراج یا ذخیره لینک استریم
- بدون دانلود محتوا
- فقط عنوان، پوستر و لینک عادی صفحه جزئیات از حساب خود کاربر

## موارد جدید — بررسی امکان‌سنجی

| مورد | شدنی؟ | یادداشت |
|---|---|---|
| پخش مستقیم خارجی کار نکرد | احتمالاً باگ نیست | ردیف «تازه‌های خارجی» از `new-films` می‌آید و **اغلب آن فیلم‌ها اصلاً نسخه پخش آنلاین ندارند** (Colony نداشت). فقط عنوان‌های `category/stream` دارند. راه‌حل: نشان‌دار کردن موارد پخش‌آنلاین‌دار |
| سرویس سوم خارجی | بله | معماری Adapter از قبل آماده است: services.json + اسکریپت کاتالوگ/جستجو/directPlay. نیاز به یک جلسه بازرسی روی آن سایت. ریسک: پلیر متفاوت (Fullscreen/Resume)، زمان Sync بیشتر، حذف تکراری بین سه منبع |
| کانال‌های زنده فارسی | مشروط | Aminema فقط وب‌سایت باز می‌کند؛ اپلیکیشن دیگر را نمی‌شود داخلش گذاشت. اگر پورتال وبی پیدا شود که خارج ایران کار کند، می‌شود یک سرویس معمولی |
| خلاصه + بازیگران روی Hover | بله، ارزان | داده از قبل در همان پاسخ‌هایی است که می‌گیریم: کارت خارجی ژانر/کشور/سال/مدت/امتیاز/کارگردان/ستارگان دارد و آیتم ایرانی `description` و `genres` |
| واچ‌لیست + نوتیفیکیشن قسمت جدید | بله | Favorites محلی موجود است؛ سایت خارجی دکمه‌های watch/favorite/watched دارد. نوتیفیکیشن با مقایسه وضعیت قسمت و یک کار پس‌زمینه، بدون سرور |

## پخش زنده — معماری پیاده‌شده در 0.12.0

به‌جای فایل جدا، هر سرویس یک بلوک اختیاری `liveTv.channels` در همان
`services.json` دارد؛ بنابراین نشست، User Agent و دامنهٔ هر منبع با Adapter
همان سرویس مشترک می‌ماند و در عین حال اضافه‌کردن شبکه فقط تغییر JSON است.

هر کانال فقط این‌ها را لازم دارد:

- `id`, `name`
- `logoUrl` — آدرس لوگوی قابل‌نمایش
- `path` — مسیر **صفحه عادی وب** همان کانال نسبت به URL سرویس

Home ردیف «پخش زنده» را با لوگوها نشان می‌دهد؛ کلیک روی هر کانال همان صفحه را
در `BrowserActivity` با `liveTheaterMode` باز می‌کند. ویدئوی HTML5 خود سایت
بدون Fullscreen API مرورگر، تمام Viewport می‌شود؛ Back مستقیم به Home و همان
کارت برمی‌گردد.

محدودیت‌هایی که باید شفاف باشند:

- فقط منبعی که **صفحه وب** است کار می‌کند؛ اگر پخش فقط داخل یک اپلیکیشن باشد
  قابل افزودن نیست.
- بعضی منابع Geo-block یا نیازمند ورود هستند؛ ورود مثل بقیه سرویس‌ها داخل
  WebView انجام می‌شود.
- هر سایت پلیر متفاوتی دارد، پس تمام‌صفحه‌شدن باید برای هر منبع جدید یک بار
  تست شود.

## بروزرسانی خودکار درون‌برنامه‌ای

شدنی است. الگوی استاندارد برای یک اپ Sideload شخصی:

1. یک فایل کوچک `update.json` روی یک میزبان ثابت: `versionCode`،
   `versionName`، آدرس APK، `sha256` و توضیح کوتاه تغییرات.
2. برنامه در Cold Start (یا روزی یک‌بار) آن را با `BuildConfig.VERSION_CODE`
   مقایسه می‌کند.
3. اگر جدیدتر بود، یک دیالوگ تلویزیونی: «نسخه X موجود است».
4. دانلود در حافظه خصوصی برنامه، بررسی `sha256`، سپس نصب با
   `PackageInstaller` و FileProvider.
5. حذف فایل دانلودشده بعد از نصب، تا حافظه جعبه پر نشود.

پیش‌نیازها و محدودیت‌های واقعی:

- مجوز `REQUEST_INSTALL_PACKAGES` و **یک‌بار** فعال‌کردن دستی
  «نصب از منابع ناشناس» برای Aminema در تنظیمات دستگاه.
- نصب کاملاً بی‌صدا **ممکن نیست** مگر برنامه Device Owner یا سیستمی باشد؛
  پس همیشه یک تأیید کاربر لازم است. همین هم خوب است: بروزرسانی خودکارِ یک
  Build خراب، بدتر از بروزرسانی دستی است.
- امضا باید یکسان بماند (هست) وگرنه به‌جای Update، نصب تازه می‌شود.

درباره میزبان: **Google Drive گزینه خوبی نیست.** برای فایل ۷۵ مگابایتی صفحه
تأیید «امکان اسکن ویروس نیست» می‌آید و لینک مستقیم پایدار نمی‌دهد.
GitHub Releases برای این کار مناسب است (لینک مستقیم، نسخه‌بندی، رایگان) —
با این ملاحظه که مخزن عمومی یعنی آدرس سرویس‌های شخصی هم عمومی می‌شود؛ اگر
مهم است، فقط APK منتشر شود یا میزبان خصوصی انتخاب شود.

نکته مرتبط: حجم فعلی هر بروزرسانی حدود ۷۵ مگابایت است چون Build دیباگ است.
یک Build ریلیز با R8 این را بسیار کم می‌کند و هر دانلود را سبک‌تر.

## دسترسی به کانال‌های تلگرام — نیاز به بررسی، نه آماده شروع

ایده: سریال‌هایی که فقط از طریق کانال تلگرام منتشر می‌شوند (فایل ویدیو، بدون
سایت رسمی)، از طریق تلگرام وب و بدون دانلود دستی تماشا شوند.

این با ParsiFlix/FilmRooz فرق بنیادی دارد و **قبل از پیاده‌سازی باید با
بازرسی واقعی روی امولاتور بررسی شود**، نه فرض:

1. **لاگین داخل WebView ممکن است مسدود باشد.** چند سرویس بزرگ به‌خاطر امنیت
   لاگین در WebViewهای غیر مرورگر اصلی را رد می‌کنند. باید تست شود که
   `web.telegram.org` لاگین کاربر را داخل WebView برنامه می‌پذیرد یا نه.
2. **پلیر ویدیوی تلگرام وب رمزگشایی سمت کلاینت دارد**؛ ممکن است `<video>`
   استاندارد نباشد یا رویدادهای Play/Fullscreen/Resume که Adapter فعلی رویشان
   حساب باز کرده متفاوت باشند.
3. **سنگین است.** تلگرام وب یک SPA کامل چت است، نه یک صفحه ساده — مستقیم در
   تضاد با هدف سبک‌سازی.
4. **کاتالوگ خودکار یا انتخاب کیفیت شدنی نیست.** نام‌گذاری فایل‌ها دستی و
   غیراستاندارد است و هر کیفیت پیام جدا؛ برخلاف FilmRooz الگوی قابل‌Parse
   وجود ندارد. در بهترین حالت یعنی مرور دستی داخل صفحه تلگرام، نه پخش مستقیم
   هوشمند.

اگر مورد ۱ و ۲ رد شوند، افزودن به‌عنوان یک سرویس معمولی (فقط WebView، بدون
Adapter هوشمند) کم‌هزینه است. لینک پیام (`t.me/channel/id`) پایدار است و
می‌تواند آدرس Recently Opened باشد.

قدم بعدی وقتی نوبتش شد: کاربر یک‌بار داخل امولاتور وارد تلگرام وب شود، بعد
DOM یک پیام ویدیویی بازرسی شود — دقیقاً همان روشی که برای دو سرویس دیگر جواب
داد.

## منابع خارجی — سه صفحه جدید بررسی و تأیید شد (روی داده واقعی)

هر چهار آدرس زیر با اسکن زنده تست شدند؛ همه HTTP 200 و با همان ساختار کارت
فعلی (`.postMeta`) به‌درستی Parse می‌شوند — نیازی به Parser جدید نیست.

### `/archive/series/` — «سریال‌های بروز شده» — **مهم‌ترین اصلاح**

این صفحه با `/archive/category/new-tv-show/` (که پیش از 0.14 استفاده می‌شد) کاملاً
فرق دارد و هر دو در منوی خود سایت جدا از هم هستند:

- `new-tv-show` = سریال‌هایی که **خودشان** تازه‌اند (مثل Gone، Overdo — هر دو
  از ۲۰۲۶)
- `/archive/series/` = سریال‌هایی که **قسمت تازه** دارند، فارغ از سال ساخت
  (نمونه واقعی: Love Island از ۲۰۱۵، Bleach از ۲۰۲۲، WWE Raw از ۱۹۹۳)

این دقیقاً همان «تازه‌های سریال» است که کاربر واقعی دنبالش است. تأیید شد که
هر آیتم برچسب قسمت/فصل هم دارد (`قسمت ۵۵ فصل سیزدهم`، `قسمت ۰۸ فصل اول`)،
یعنی همان کار «وضعیت قسمت زیر نام سریال» را هم با همین منبع می‌شود جواب داد.

**انجام و منتشر شد در 0.14.0:** منبع سریال ردیف «تازه‌های خارجی» از
`new-tv-show` به `/archive/series/` تغییر کرد.

### `/archive/category/featured-films/` — «فیلم‌های برگزیده»

با `new-films` هم‌پوشانی جزئی دارد (مثلاً Colony و A Toxic Love Story در هر
دو هستند) ولی فیلتر متفاوتی دارد و بقیه‌اش را نشان نمی‌دهد. الگوریتم
انتخابش برای ما مهم نیست. **تصمیم:** به‌عنوان منبع دوم فیلم خارجی اضافه شود
(بالای «همه» یا به‌صورت یک تب/ردیف جدا «تازه‌های برگزیده»)، با
`distinctBy(contentUrl)` که از قبل داریم، تکراری‌ها حذف می‌شوند.

### `/archive/playlist/show/most-popular-tv-shows/` — «سریال‌های محبوب»

**این تازه نیست، Trend سایت است** (مثال واقعی: House of the Dragon،
Little House on the Prairie). کاربر صراحتاً گفته این را نباید با «تازه‌ها»
قاطی کرد. اولویت پایین‌تر از دو مورد بالا؛ اگر اضافه شود باید ردیف/برچسب
جدا «پرطرفدار» باشد، نه بخشی از «تازه‌های خارجی».

### ترتیب اولویت طبق نظر کاربر

1. **`/archive/series/` جایگزین `new-tv-show` — انجام شد در 0.14.0**
2. `featured-films` به فیلم خارجی اضافه شود
3. **`most-popular-tv-shows` به‌عنوان ردیف جدا — انجام شد در 0.14.0**

## بروزرسانی خودکار درون‌برنامه‌ای — انجام شد در 0.10.0

پیاده‌سازی و تست شد: چک در Cold Start از GitHub Releases API (بدون فایل
مانیفست جدا)، دانلود با نوار پیشرفت، بررسی sha256، نصب با
`PackageInstaller`/`FileProvider`، دکمه «بعداً» با یادآوری فقط برای نسخه
بعدی، و دکمه بررسی دستی در Settings.

**تصمیم عمدی:** فشرده‌سازی R8 از این نسخه جدا نگه داشته شد. Build همچنان
دیباگ و بدون Minify است تا شناسه بسته و امضا با نصب فعلی یکی بماند. R8 یک
تغییر سطح کل Build است (می‌تواند WebView JS Bridge، Coil، Serialization
Reflection را بشکند) و فقط با امولاتور قابل تأیید کامل نیست؛ به یک نسخه جدا
با تست دقیق‌تر موکول شد.
